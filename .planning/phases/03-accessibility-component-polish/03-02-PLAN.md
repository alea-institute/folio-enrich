---
phase: 03-accessibility-component-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/index.html
autonomous: false
requirements: [A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-05, A11Y-06]

must_haves:
  truths:
    - "Every text-on-background pair listed as FAIL in 03-AUDIT-REPORT.md now has ratio >= 4.5:1 (or >= 3.0:1 if confirmed large-text-only)"
    - "`--text-dim` in all themes achieves ≥ 4.5:1 against `--bg` and `--surface` (target 5.5:1 per D-04)"
    - "`--accent` in all themes achieves ≥ 4.5:1 against `--bg` and `--surface`"
    - "All 26 branch tints at 12% opacity on `--bg` yield ≥ 4.5:1 contrast with the applicable text color (via --text or a per-branch override)"
    - "Re-running `node scripts/contrast-audit.mjs` shows zero entries in `## Failing Pairs` for body text"
    - "User visually confirms no regressions on Railway deployment"
  artifacts:
    - path: "frontend/index.html"
      provides: "Updated --text-dim, --accent values + any per-branch text overrides, zero contrast failures in body-text tier"
      contains: "data-theme"
  key_links:
    - from: "frontend/index.html [data-theme=*] blocks"
      to: "scripts/contrast-audit.mjs output"
      via: "re-audit confirms zero failures"
      pattern: "## Failing Pairs"
---

<objective>
Apply surgical fixes to token values in `frontend/index.html` based on the failing-pairs list in `03-AUDIT-REPORT.md`. Adjust foreground text colors (not backgrounds) per D-02: darken/lighten `--text-dim`, `--accent`, add per-branch text overrides where 26 branch tints fail. Re-run the audit to prove every fix lands. Push to Railway so the traveling user can verify visually.

Purpose: Close the WCAG AA gaps identified in Plan 03-01. Legal-professional audience requires non-negotiable contrast.
Output: Updated frontend/index.html with WCAG-compliant tokens + passing audit report.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-accessibility-component-polish/03-CONTEXT.md
@.planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md
@.planning/phases/03-accessibility-component-polish/03-01-SUMMARY.md
@.planning/phases/01-css-variable-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Exact line ranges for token definitions in frontend/index.html (as of this plan's creation). -->
<!-- Verify with grep before editing; line numbers may have shifted. -->

[data-theme="dark"]              lines 62-177   (core tokens lines 65-80; branch tokens lines 148-177)
[data-theme="light"]             lines 180-295  (core tokens lines 183-198; branch tokens lines 266-295)
[data-theme="mixed"]             lines 298-409  (core tokens lines 301-314; branch tokens lines 380-408)
[data-theme="mixed"] .panel-*    lines 412-495  (core tokens lines 416-429; branch tokens lines 466-494)

Token addresses:
  --text-dim dark:        line 71  currently `var(--gray-600)` which is #8b8fa3
  --text-dim light:       line 189 currently `#5c6070`
  --text-dim mixed root:  line 307 currently `var(--gray-600)` (#8b8fa3)
  --text-dim mixed light: line 422 currently `#5c6070`

  --accent dark:          line 72  currently `var(--blue-400)` (#6c8cff)
  --accent light:         line 190 currently `#2d5ee0`
  --accent mixed root:    line 308 currently `var(--blue-400)` (#6c8cff)
  --accent mixed light:   line 423 currently `#2d5ee0`

  --bg dark:     var(--gray-950) = #0f1117
  --bg light:    #ffffff
  --bg mixed root: var(--gray-950) = #0f1117
  --bg mixed-light panels: #ffffff

Known priors from Phase 1:
- `--text-dim` on `--bg` dark (#8b8fa3 on #0f1117) ≈ 5.0:1 — may be marginal
- `--accent` light (#2d5ee0 on #ffffff) ≈ 5.1:1 — already tuned
- `--accent` dark (#6c8cff on #0f1117) ≈ should pass; verify in report

Per-branch text override strategy (if needed per D-03):
Add rules AFTER the branch color definitions, scoped by `[data-theme="X"] [data-branch="branch-name"]`, overriding `--text-on-tint` or using a direct `color:` on the annotation selector. Use the simplest approach that works — prefer `color-mix()` with `--text` before introducing new tokens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply contrast fixes from audit report to frontend/index.html</name>
  <files>frontend/index.html</files>
  <read_first>
    - .planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md (the full failing-pairs list + recommended fixes)
    - frontend/index.html (lines 60-500 — the four [data-theme=*] blocks)
    - .planning/phases/03-accessibility-component-polish/03-CONTEXT.md (D-02 through D-05)
  </read_first>
  <action>
    Read `03-AUDIT-REPORT.md` and for each entry in `## Failing Pairs` apply a fix directly in `frontend/index.html`.

    **Fix rules (in order of preference per D-02):**
    1. If `--text-dim` fails on `--bg` or `--surface` in a theme: darken (light theme) or lighten (dark theme) the value until ratio ≥ 5.5:1 against `--bg`. Update the value in the relevant `[data-theme=*]` block AND in the `[data-theme="mixed"] .panel-*` override block (if applicable). Keep both the primary and mixed definitions in sync.
    2. If `--accent` fails: same approach. Adjust hex value until ratio ≥ 5.5:1 against `--bg`. Preserve the blue family identity (stay within the blue hue range).
    3. If `--accent-dim` fails: same approach.
    4. If a branch tint fails (text on 12% branch-tinted bg): add a per-branch text override per D-03. Use the simplest selector:
       ```
       [data-theme="dark"] .annotation-span[data-branch="actor-player"] { color: <hex>; }
       ```
       Place all such overrides in a new CSS comment-delimited section `/* Per-branch text overrides for WCAG AA */` placed immediately after the branch color definitions in each theme block. Only add overrides for branches that actually fail — do NOT add overrides for passing branches.

    **Constraints:**
    - Do NOT change any background token values (`--bg`, `--surface`, `--surface2`, `--surface3`, `--modal-bg`, `--tooltip-bg`). Per D-02.
    - Do NOT add new npm dependencies.
    - Do NOT remove or restructure existing tokens. Only update values.
    - When picking new hex values, target 5.5:1 body text (D-04 margin), not bare minimum 4.5:1. Use the contrast-audit.mjs script's `contrastRatio()` function iteratively if needed (call via `node -e "import('./scripts/contrast-audit.mjs').then(m => console.log(m.contrastRatio('#xxxxxx', '#yyyyyy')))"`).
    - For dark theme, lighten text toward white (e.g., `--gray-600` #8b8fa3 → something like #a8acbe for ~6:1 on #0f1117).
    - For light theme, darken text toward black (e.g., #5c6070 → #4a4e5c or lower for ~6:1 on #ffffff).
    - If the report shows NO failing body-text pairs for a theme, leave that theme's tokens untouched.

    **Keep token sync:**
    After changing any token, verify all four theme contexts (dark, light, mixed-root, mixed-light-panels) remain internally consistent. Mixed theme shares token values with dark at root and light at panels — if you change light theme, update the mixed panel override to match.

    **Large-text exception (D-05):**
    If the audit report flags a pair as "LARGE-ONLY" (ratio 3.0-4.4), check what element uses that token. If the token is only used for headings/tab-labels/panel-headers (≥18pt regular or ≥14pt bold), leave as-is and add a comment:
    ```
    /* --text-dim: X — LARGE-TEXT-ONLY per D-05 (only used for panel headers ≥18pt) */
    ```
    If the token is used for body text anywhere, apply the fix.

    After all fixes applied, re-run `node scripts/contrast-audit.mjs` from project root to regenerate the audit report. Verify the new report shows zero body-text failures.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-enrich" && node scripts/contrast-audit.mjs && grep -A1 "^## Failing Pairs" .planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md | tail -n +2 | grep -c "^- " || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - After re-running the audit, `## Failing Pairs` section in 03-AUDIT-REPORT.md is empty OR contains only entries explicitly marked as large-text-only (D-05)
    - Grep-verifiable: `grep -c "data-theme" frontend/index.html` returns ≥4 (theme blocks intact)
    - Grep-verifiable: background tokens unchanged — `grep -c "gray-950\|#ffffff" frontend/index.html` returns same count as before (no bg replaced)
    - Grep-verifiable: if per-branch overrides were added, `grep -c "Per-branch text overrides for WCAG AA" frontend/index.html` returns ≥1
    - Running `node --check` equivalent for CSS: browser DevTools shows no CSS parse errors (manual spot-check)
    - Backend tests still pass: `cd backend && .venv/bin/python -m pytest tests/ -v --co -q` collects without error (sanity check that nothing unrelated broke)
  </acceptance_criteria>
  <done>All body-text contrast failures from 03-AUDIT-REPORT.md are resolved in frontend/index.html; re-run audit confirms zero failures; per-branch overrides added only where needed.</done>
</task>

<task type="auto">
  <name>Task 2: Commit + push to Railway for user verification</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (verify changes are staged correctly)
    - .planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md (confirm post-fix state)
  </read_first>
  <action>
    Stage the frontend changes and the regenerated audit report:

    ```
    cd "/home/damienriehl/Coding Projects/folio-enrich"
    git add frontend/index.html .planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md
    git status
    ```

    Commit with a clear message documenting what was fixed:

    ```
    git commit -m "fix(a11y): resolve WCAG AA contrast failures in theme tokens

    - Adjusted --text-dim in {themes} to meet 5.5:1 body text target
    - Adjusted --accent in {themes} to meet 5.5:1 body text target
    - Added per-branch text overrides for N branches failing on tinted backgrounds
    - Re-audit confirms zero body-text failures across dark/light/mixed

    Refs: A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-05, A11Y-06"
    ```

    Push to the current branch:
    ```
    git push
    ```

    Railway auto-deploys on push to this branch. Poll deployment status if possible, or simply confirm `git push` succeeded.

    Do NOT force-push. Do NOT modify git config.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-enrich" && git log -1 --format="%s" | grep -E "fix\\(a11y\\)|A11Y" && git status --porcelain frontend/index.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format="%s"` matches pattern `fix(a11y):` or contains `A11Y`
    - `git status --porcelain frontend/index.html` returns 0 (file is committed, no uncommitted changes)
    - `git push` exits 0
    - Current branch's remote tracking is up to date: `git status` shows "Your branch is up to date with 'origin/...'"
  </acceptance_criteria>
  <done>Contrast fixes committed and pushed. Railway deployment triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies contrast fixes on Railway</name>
  <files>frontend/index.html</files>
  <read_first>
    - .planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md (the fixes applied)
  </read_first>
  <action>
    Pause execution and prompt the user to visually verify the contrast fixes on the Railway deployment URL.

    Present the what-built description, step-by-step verification instructions, and wait for resume signal.

    Do NOT proceed until user types "approved" or provides specific feedback.

    If user reports issues, categorize each as:
    (a) audit-report miss — the audit script missed a failing pair; fix requires re-running audit with expanded scope
    (b) aesthetic preference — contrast passes WCAG but user dislikes the look; adjust value toward user's preferred balance while keeping ≥5.5:1
    (c) regression — something that worked before is now broken; identify root cause and fix

    Return feedback to the orchestrator for follow-up plan or for inclusion in 03-02-SUMMARY.md.
  </action>
  <what-built>
    Adjusted `--text-dim`, `--accent`, and per-branch text colors across dark/light/mixed themes to meet WCAG AA (4.5:1 body text, 3:1 large text). The automated audit script confirms zero failures. Now needs human eye-check that the adjustments don't feel "wrong" aesthetically or break the existing design language.
  </what-built>
  <how-to-verify>
    On the Railway deployment URL:

    1. **Dark theme check:**
       - Toggle to Dark theme via header bar
       - Open a document, look at secondary text (concept definitions, meta labels, timestamps)
       - Confirm: text is readable, feels natural, doesn't look washed-out or "too bright"
       - Click several concept annotations across different branches
       - Confirm: text on branch-tinted backgrounds is legible

    2. **Light theme check:**
       - Toggle to Light theme
       - Same checks: secondary text readable, not "too pale"
       - Check accent links: hover states visible, clickable things stand out
       - Click branch annotations — text legible on tinted backgrounds

    3. **Mixed theme check:**
       - Toggle to Mixed theme (default)
       - Left panel (dark): secondary text and accent legible
       - Right panel (light): same checks
       - Tooltips appear with correct light styling over concepts

    4. **Branch sweep:**
       - Look at several annotations using different branches (actor-player, legal-authorities, financial-concepts, etc.)
       - Confirm: branch identity (hue) still visible despite any per-branch text overrides
       - No branch feels "dead" or washed-out

    Expected outcome: Everything looks readable and balanced. Colors may be slightly different from before — that's intentional.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify requires human approval — no automated verify"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" or describes specific issues to address
    - If issues raised, they are categorized as: (a) audit-report miss, (b) aesthetic preference, (c) regression
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g., "--text-dim too dark in light mode, try something between old and new")</resume-signal>
  <done>User has approved the contrast adjustments on Railway OR provided actionable feedback categorized for follow-up.</done>
</task>

</tasks>

<verification>
- 03-AUDIT-REPORT.md shows zero body-text failures post-fix
- `--text-dim`, `--accent`, `--accent-dim` tokens achieve ≥5.5:1 where possible (≥4.5:1 minimum)
- Per-branch overrides exist only for branches that originally failed (D-03)
- Background tokens unchanged (D-02)
- User approval on Railway deployment
</verification>

<success_criteria>
- All 6 A11Y requirements satisfied:
  - A11Y-01: Dark theme — all body text ≥4.5:1
  - A11Y-02: Light theme — all body text ≥4.5:1
  - A11Y-03: Mixed theme — all body text ≥4.5:1
  - A11Y-04: All 26 branch labels legible in all 3 themes
  - A11Y-05: Large text ≥3:1 in all themes
  - A11Y-06: --text-dim and --accent have safe margins above AA
- Fixes committed, pushed, deployed to Railway
- User visually approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-accessibility-component-polish/03-02-SUMMARY.md` using the standard template. Include: count of tokens adjusted, list of per-branch overrides added, before/after contrast ratios for each changed token, user feedback.
</output>
