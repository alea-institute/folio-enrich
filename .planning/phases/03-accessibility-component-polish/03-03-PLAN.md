---
phase: 03-accessibility-component-polish
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/index.html
autonomous: false
requirements: [CMPT-01, CMPT-02, CMPT-03]

must_haves:
  truths:
    - "Every modal opened in Dark/Light/Mixed themes displays with theme-appropriate background, text, border, and input styling — no dark-on-dark or light-on-light illegibility"
    - "Every tooltip (concept, individual, property, triple, branch overlay) renders correctly in Dark/Light/Mixed themes"
    - "Scrollbars use the active theme's `color-scheme` + `--scrollbar-thumb`/`--scrollbar-thumb-hover` values in all three modes"
    - "User visually confirms all components render correctly across all themes on Railway"
  artifacts:
    - path: "frontend/index.html"
      provides: "Component-theming gap fixes — any missed modal/tooltip/scrollbar selectors now wired to theme tokens"
      contains: "data-theme"
    - path: ".planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md"
      provides: "Verification checklist of every modal and tooltip tested per theme with status"
      contains: "## Modal Verification Matrix"
  key_links:
    - from: "frontend/index.html .modal and .concept-tooltip CSS rules"
      to: "CSS custom properties (--modal-*, --tooltip-*)"
      via: "var() references resolve per-theme"
      pattern: "var\\(--modal-|var\\(--tooltip-"
---

<objective>
Verify every modal, tooltip, and scrollbar renders correctly in Dark/Light/Mixed themes on Railway. Fix any gaps discovered (missing token references, hardcoded colors still sneaking through, broken hover/focus states, missing borders, contrast issues). Per D-06: verify and fix only — do NOT redesign working components. Most of this work may be "pass" with only minor adjustments.

Purpose: Ensure overlay components theme correctly. Modals and tooltips are the most likely places for dark-on-dark bugs since they float above other content with their own context.
Output: Verification checklist + any targeted fixes to frontend/index.html.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-accessibility-component-polish/03-CONTEXT.md
@.planning/phases/03-accessibility-component-polish/03-02-SUMMARY.md

<interfaces>
<!-- Component selectors and their token addresses in frontend/index.html. -->

Modal selectors (verify each themes correctly):
- .modal                     lines 2410-2421  background: var(--modal-bg), color: var(--modal-text), border: var(--modal-border)
- .modal h3                  line 2423-2427
- .modal .form-group label   line 2433
- .modal select/input        lines 2443-2449   var(--modal-input-bg), var(--modal-input-border)
- .modal button.secondary    line 2456+
- .modal-overlay             lines 2398-2408

Known modals in the app (open via UI):
- Settings modal (gear icon)
- Upload/enrich modal (main CTA)
- Ollama setup wizard modal
- Synthetic doc generator modal
- Export modal
- Confirmation dialogs

Tooltip selectors:
- .concept-tooltip           lines 1478-1495   var(--tooltip-bg), var(--tooltip-border), var(--tooltip-text)
- .tooltip-branch            line 1497
- .tooltip-label             line 1505         var(--tooltip-text)
- .tooltip-matched-as        line 1506         var(--text-dim)
- .tooltip-definition        line 1517         var(--tooltip-text)
- .tooltip-synonyms          line 1518         var(--text-dim)
- .tooltip-meta              line 1520
- .tooltip-iri a             line 1521         var(--accent)

Known tooltip types (hover/click triggers):
- Concept tooltip (hover annotated text)
- Individual tooltip (hover entity spans)
- Property tooltip (hover property underlines)
- Triple tooltip (Triples tab cross-link badges)
- Branch overlay tooltip (multi-branch stacked indicator)

Scrollbar rules:
- Line 2606: `::-webkit-scrollbar { width: 5px; }` (global)
- Line 2607: `::-webkit-scrollbar-track { background: transparent; }`
- Line 2608: `::-webkit-scrollbar-thumb { background: var(--border); ... }`
- Line 2609: `::-webkit-scrollbar-thumb:hover { background: var(--accent-dim); }`
- Line 795-796: .panel-right .panel-body scrollbar — uses --scrollbar-thumb / --scrollbar-thumb-hover
- Line 808-809: .panel-detail .panel-body scrollbar — same
- Line 2429-2430: .modal scrollbar — same

Token mapping: scrollbars pick up `color-scheme: dark|light` from the [data-theme=*] block, which styles native Firefox scrollbars + Chrome fallbacks automatically. Custom `::-webkit-scrollbar-*` rules use `--scrollbar-thumb` variants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systematic component audit + create verification checklist</name>
  <files>.planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md</files>
  <read_first>
    - frontend/index.html (lines 1478-1620 for tooltip CSS, lines 2398-2520 for modal CSS, lines 2600-2620 for scrollbar CSS)
    - .planning/phases/03-accessibility-component-polish/03-CONTEXT.md (D-06 scope)
  </read_first>
  <action>
    Grep `frontend/index.html` to enumerate every modal, tooltip, and scrollbar-related CSS rule. Produce a verification checklist that lists:
    1. Every modal class and which modals use it (cross-ref JS to find openModal() calls)
    2. Every tooltip class and trigger
    3. Every scrollbar rule and scope

    Write the checklist to `.planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md`:

    ```markdown
    # Phase 03 Component Verification Checklist

    ## Modal Verification Matrix
    | Modal | Trigger | Dark | Light | Mixed | Notes |
    |-------|---------|------|-------|-------|-------|
    | Settings modal | gear icon | [ ] | [ ] | [ ] | |
    | ... | ... | | | | |

    ## Tooltip Verification Matrix
    | Tooltip | Trigger | Dark | Light | Mixed | Notes |
    |---------|---------|------|-------|-------|-------|
    | Concept tooltip | hover annotation | [ ] | [ ] | [ ] | |
    | ... | ... | | | | |

    ## Scrollbar Verification Matrix
    | Scrollable Area | Dark | Light | Mixed | Notes |
    |-----------------|------|-------|-------|-------|
    | Main doc panel (.panel-content) | [ ] | [ ] | [ ] | |
    | Right panel concept list | [ ] | [ ] | [ ] | |
    | Detail panel | [ ] | [ ] | [ ] | |
    | Modal content | [ ] | [ ] | [ ] | |
    | ... | | | | |

    ## Hardcoded Colors Audit
    (Grep for any remaining hex values in modal/tooltip CSS blocks)
    ```

    Grep commands to run and record output:
    ```
    grep -n "modal\\b" frontend/index.html | grep -iE "openModal|modal-overlay|\\.modal\\s" | head -30
    grep -n "tooltip\\b" frontend/index.html | grep -iE "showTooltip|\\.concept-tooltip" | head -30
    grep -n "#[0-9a-fA-F]\\{6\\}\\|#[0-9a-fA-F]\\{3\\}\\b" frontend/index.html | grep -iE "modal|tooltip|scrollbar" | head -20
    ```

    For each hardcoded hex in modal/tooltip/scrollbar contexts, add it to the "Hardcoded Colors Audit" section with the line number and verdict (intentional vs. needs-token).

    This task produces the checklist only. The next task performs visual verification and fixes.
  </action>
  <verify>
    <automated>test -f "/home/damienriehl/Coding Projects/folio-enrich/.planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md" && grep -c "^## " "/home/damienriehl/Coding Projects/folio-enrich/.planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md"</automated>
  </verify>
  <acceptance_criteria>
    - File `03-03-COMPONENT-CHECKLIST.md` exists
    - Grep-verifiable: `grep -c "^## " 03-03-COMPONENT-CHECKLIST.md` returns ≥4 (Modal Matrix, Tooltip Matrix, Scrollbar Matrix, Hardcoded Colors Audit)
    - Grep-verifiable: `grep -c "^| " 03-03-COMPONENT-CHECKLIST.md` returns ≥10 (substantial tables)
    - Grep-verifiable: `grep -c "\\[ \\]" 03-03-COMPONENT-CHECKLIST.md` returns ≥9 (3 themes × at least 3 components)
    - Checklist enumerates at least 4 modals, 4 tooltip types, and 4 scrollable areas
  </acceptance_criteria>
  <done>Component verification checklist exists with all modals, tooltips, and scrollbars enumerated and a hardcoded-colors audit performed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any component theming gaps + push to Railway</name>
  <files>frontend/index.html</files>
  <read_first>
    - .planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md (the hardcoded colors audit findings)
    - frontend/index.html (lines 1478-1620, 2398-2520, 2600-2620)
  </read_first>
  <action>
    For each item flagged in the "Hardcoded Colors Audit" section of the checklist that is marked `needs-token`:
    - Replace the hardcoded hex with the appropriate theme token (`var(--modal-*)`, `var(--tooltip-*)`, `var(--scrollbar-*)`, or `var(--border)`/`var(--text)`/etc.)
    - If a needed token does not exist, add it to the component tokens section in each of the 4 theme contexts (dark, light, mixed root, mixed panel overrides)

    For any CSS rule that uses `rgba(...)` literals inside modal/tooltip/scrollbar scopes that do NOT reference a CSS var:
    - Evaluate: is the alpha value the reason? If yes, use `color-mix(in srgb, var(--token) N%, transparent)` instead.
    - If it's a box-shadow (e.g., `box-shadow: 0 12px 40px rgba(0,0,0,0.15)`), leave it — shadows are theme-agnostic.

    **Specific items to verify work:**
    1. Modal overlay backdrop `rgba(0,0,0,0.3)` at line 2401 — verify it reads correctly in light theme (may need theme-aware backdrop). If backdrop looks washed out on light bg, leave as-is for now per D-06 (verify only, don't redesign).
    2. Concept tooltip box-shadow — verify visible on all backgrounds in all themes.
    3. Scrollbar rules at line 2606-2609 — verify global `::-webkit-scrollbar-thumb { background: var(--border); }` looks correct in all themes. If thumb is nearly invisible in some theme, swap to `var(--scrollbar-thumb)` to use the already-defined token.

    After fixes applied, stage + commit + push:

    ```
    cd "/home/damienriehl/Coding Projects/folio-enrich"
    git add frontend/index.html .planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md
    git commit -m "fix(theme): wire component token gaps + verification checklist

    - Replaced hardcoded colors in modal/tooltip/scrollbar CSS with theme tokens
    - {List specific fixes made, or 'No gaps found' if audit was clean}

    Refs: CMPT-01, CMPT-02, CMPT-03"
    git push
    ```

    If the checklist audit found NO hardcoded-color gaps and NO CSS issues, still commit the checklist file with a commit message noting "audit clean, no fixes required".
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-enrich" && git log -1 --format="%s" | grep -iE "CMPT|theme|component" && git status --porcelain frontend/index.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format="%s"` contains CMPT-01, CMPT-02, CMPT-03 reference or "theme" keyword
    - `git status --porcelain frontend/index.html` returns 0 (committed, clean)
    - `git status --porcelain` shows nothing in `.planning/phases/03-accessibility-component-polish/` (checklist committed)
    - Grep-verifiable: if new tokens were added, `grep -c "\\-\\-modal-\\|\\-\\-tooltip-\\|\\-\\-scrollbar-" frontend/index.html` increased
    - Grep-verifiable: `git push` exits 0
    - Zero hardcoded hex colors in modal/tooltip CSS rule bodies (except box-shadow rgba which is intentional): `grep -E "\\.modal|\\.concept-tooltip" frontend/index.html | grep -cE "background:\\s*#[0-9a-fA-F]|color:\\s*#[0-9a-fA-F]"` returns 0
  </acceptance_criteria>
  <done>All component-theming gaps identified in checklist are fixed OR documented as intentional. Changes committed and pushed to Railway.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies modals, tooltips, scrollbars across all themes on Railway</name>
  <files>frontend/index.html</files>
  <read_first>
    - .planning/phases/03-accessibility-component-polish/03-03-COMPONENT-CHECKLIST.md
  </read_first>
  <action>
    Pause execution and prompt the user to verify modals, tooltips, and scrollbars on the Railway deployment.

    Present the what-built description, the full 3-section verification matrix (modals × tooltips × scrollbars × 3 themes), and wait for resume signal.

    Do NOT proceed until user types "approved" or lists specific issues.

    If user reports issues, categorize each by component scope:
    - Modal issue → fix in modal CSS (lines 2398-2520)
    - Tooltip issue → fix in tooltip CSS (lines 1478-1620)
    - Scrollbar issue → fix in scrollbar rules (lines 2606-2609 global; 795-809 panels)

    Update the component checklist with results (check boxes, note issues). Return summary to orchestrator.
  </action>
  <what-built>
    Systematic verification + gap fixes for all modals, tooltips, and scrollbars across Dark/Light/Mixed themes. Any hardcoded colors found in component CSS were replaced with theme tokens. Now needs human verification on Railway that every overlay component renders correctly in every theme.
  </what-built>
  <how-to-verify>
    On the Railway deployment URL, work through the verification matrix:

    **Modals — for each theme (Dark, Light, Mixed):**
    1. Click gear icon → Settings modal. Check: background matches theme context, text readable, form inputs styled correctly, buttons visible with proper contrast, close button clickable.
    2. Click Upload/Enrich → main upload modal. Same checks.
    3. (If available) Open Ollama setup wizard. Same checks.
    4. (If available) Open synthetic doc generator. Same checks.
    5. (If available) Open Export modal. Same checks.

    **Tooltips — for each theme:**
    1. Hover over any concept annotation. Tooltip should appear with theme-appropriate background, border, and text. Check: definition readable, branch pill visible, IRI link has proper accent color, feedback buttons visible.
    2. Hover over an individual (entity/citation) span. Individual tooltip should appear with theme styling.
    3. Hover over a property underline. Property tooltip should appear.
    4. Click Triples tab, hover over a triple cross-link badge. Triple tooltip should appear.
    5. (If visible) Hover over a multi-branch overlay indicator. Branch overlay tooltip should appear.

    **Scrollbars — for each theme:**
    1. Scroll the main document panel. Scrollbar thumb visible, thumb hover state works.
    2. Scroll the right concept list panel. Same.
    3. Scroll the detail panel. Same.
    4. Inside a modal with long content (if any), scroll. Scrollbar visible.
    5. Any scrollbar should use the correct light/dark thumb per theme — no dark thumb on light bg or vice versa.

    **Cross-theme toggling:**
    Toggle themes in header bar rapidly. Confirm: all open modals/tooltips update immediately, no stale colors, no flashes.

    Expected outcome: Every modal/tooltip/scrollbar renders correctly in all 3 themes with no illegibility, no missing borders, no stale colors. Report specific issues with: component name + theme + what's wrong.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify requires human approval — no automated verify"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" OR describes specific issues per component+theme
    - All modals verified in all 3 themes (12+ combinations)
    - All tooltip types verified in all 3 themes
    - Scrollbars verified in all 3 themes for all major scroll areas
  </acceptance_criteria>
  <resume-signal>Type "approved" or list issues as "[component] [theme]: [description]" (e.g., "Settings modal Dark: input border invisible")</resume-signal>
  <done>User has approved all component×theme combinations on Railway OR provided specific, scoped feedback for targeted follow-up fixes.</done>
</task>

</tasks>

<verification>
- All component selectors enumerated in checklist
- Hardcoded colors in modal/tooltip CSS rule bodies: zero (outside intentional shadows)
- User visually confirms all 12+ modal×theme combinations render correctly
- User confirms all tooltip types in all 3 themes
- User confirms scrollbars match active theme
</verification>

<success_criteria>
- CMPT-01: Modals render with theme-appropriate colors in Dark/Light/Mixed — user approved
- CMPT-02: Tooltips render with theme-appropriate colors in Dark/Light/Mixed — user approved
- CMPT-03: Scrollbars themed per mode via color-scheme + --scrollbar-thumb — user approved
- Component checklist committed with verification status
</success_criteria>

<output>
After completion, create `.planning/phases/03-accessibility-component-polish/03-03-SUMMARY.md` using the standard template. Include: checklist audit findings, list of fixes applied (or "audit clean"), user feedback summary, final component verification matrix state.
</output>
