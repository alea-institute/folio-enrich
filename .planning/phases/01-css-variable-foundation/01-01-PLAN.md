---
phase: 01-css-variable-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
autonomous: true
requirements:
  - CSSF-02
  - CSSF-03
  - CSSF-04
  - CSSF-05
  - CSSF-06
  - CSSF-07

must_haves:
  truths:
    - "Palette primitives exist in :root and contain raw hex values that never change between themes"
    - "Semantic tokens exist under [data-theme='dark'], [data-theme='light'], and [data-theme='mixed'] selectors"
    - "Mixed theme has scoped overrides on .panel-right and .panel-detail that redefine semantic tokens to light values"
    - "All 26 branch colors have per-theme variable definitions (78 total branch color values)"
    - "data-theme='mixed' is set on the <html> element for backward compatibility"
  artifacts:
    - path: "frontend/index.html"
      provides: "Two-layer token system with three theme variable sets"
      contains: "[data-theme=\"dark\"]"
  key_links:
    - from: ":root palette primitives"
      to: "[data-theme] semantic tokens"
      via: "var() references from semantic to primitive"
      pattern: "--bg:\\s*var\\(--gray"
    - from: "[data-theme='mixed'] root"
      to: "[data-theme='mixed'] .panel-right"
      via: "scoped override selectors"
      pattern: "\\[data-theme=\"mixed\"\\]\\s+\\.panel-right"
---

<objective>
Define the complete CSS variable token system: palette primitives in :root, semantic tokens for all three themes (dark, light, mixed), and 78 branch color variables (26 branches x 3 themes). Add data-theme="mixed" to the HTML element for backward compatibility.

Purpose: This creates the variable infrastructure that Plan 02 will consume when replacing all hardcoded color values. Without this foundation, there are no variables to reference.

Output: frontend/index.html with complete token definitions added to the <style> block. No hardcoded colors are replaced yet -- this plan only DEFINES the variables and theme selectors.

Note on D-07 ("one big commit"): This plan is one of three sequential plans that together implement D-07's intent. Each plan produces a non-breaking intermediate state (Plan 01: define variables, Plan 02: replace colors, Plan 03: audit + verify). No plan leaves the app in a broken state, satisfying D-07's goal of avoiding partial/mixed states.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-variable-foundation/01-CONTEXT.md
@.planning/phases/01-css-variable-foundation/01-RESEARCH.md

<interfaces>
<!-- Current :root variables (lines 9-41 of frontend/index.html) -->
<!-- These 27 variables are the ONLY existing CSS custom properties defined at :root -->

From frontend/index.html :root block:
```css
:root {
  --bg: #0f1117;
  --surface: #1a1d27;
  --surface2: #242836;
  --surface3: #2e3348;
  --border: #2e3348;
  --text: #e4e6f0;
  --text-dim: #8b8fa3;
  --accent: #6c8cff;
  --accent-dim: #4a5fa0;
  --green: #4caf7c;
  --orange: #e8a54c;
  --red: #e05555;
  --cyan: #5ec4d4;
  --purple: #b07ee8;
  --highlight: rgba(108,140,255,0.15);
  --highlight-confirmed: rgba(108,140,255,0.22);
  --highlight-preliminary: rgba(108,140,255,0.08);
  --highlight-rejected: rgba(224,85,85,0.08);
  --branch-actor: #5ec4d4;
  --branch-area-of-law: #e8a54c;
  --branch-document: #b07ee8;
  --branch-engagement: #4caf7c;
  --branch-event: #e05555;
  --branch-location: #6c8cff;
  --branch-governmental: #e887a0;
  --branch-legal-entity: #7bed9f;
  --branch-industry: #f0c040;
  --branch-service: #50c8ff;
  --branch-default: #8b8fa3;
}
```

From frontend/index.html .panel-right (line 335) and .panel-detail (line 364):
```css
/* These are the light-mode overrides -- identical in both selectors */
--bg: #ffffff;
--surface: #f5f6f8;
--surface2: #ecedf1;
--surface3: #e2e4ea;
--border: #d0d2da;
--text: #1a1a2e;
--text-dim: #6b6b80;
--accent: #3b64e0;
--accent-dim: #5a7ae8;
--green: #16a34a;
--orange: #d97706;
--red: #dc2626;
--cyan: #0891b2;
--purple: #7c3aed;
--highlight: rgba(59,100,224,0.10);
--highlight-confirmed: rgba(59,100,224,0.15);
```

From frontend/index.html BRANCH_COLORS JS object (line 3139):
```javascript
/* These are the 27 dark/WCAG-safe branch colors used in JS and in the data-branch CSS rules */
'Actor / Player': '#1e6fa0',
'Area of Law': '#1a5276',
'Asset Type': '#6b5600',
'Communication Modality': '#7b4d93',
'Currency': '#7a5a00',
'Data Format': '#4a5568',
'Document / Artifact': '#9c4a10',
'Document Metadata': '#8b5e3c',
'Engagement Terms': '#10613a',
'Event': '#b91c1c',
'Financial Concepts and Metrics': '#6e4b00',
'FOLIO Type': '#6b5c00',
'Forums and Venues': '#7b2d8e',
'Governmental Body': '#1a6091',
'Industry': '#065550',
'Language': '#7a3b10',
'Legal Authorities': '#8b1a1a',
'Legal Entity': '#085e40',
'Legal Use Cases': '#4a3570',
'Location': '#105560',
'Matter Narrative': '#6d3580',
'Matter Narrative Format': '#1a6894',
'Objectives': '#b03020',
'Service': '#065e4e',
'Standards Compatibility': '#4a5a6a',
'Status': '#864a08',
'System Identifiers': '#3d4d5a',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define palette primitives and semantic token system for three themes</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (lines 1-42 for current :root, lines 335-395 for panel overrides, lines 655-715 for graph modal colors, lines 1001-1060 for data-branch rules, lines 2003-2070 for modal colors, lines 3139-3167 for BRANCH_COLORS JS)
    - .planning/phases/01-css-variable-foundation/01-RESEARCH.md (Architecture Patterns section for token system structure, Semantic Token Granularity table, Branch Color Variable Pattern)
    - .planning/phases/01-css-variable-foundation/01-CONTEXT.md (Decisions D-01 through D-06)
  </read_first>
  <action>
**IMPORTANT: Do NOT replace any existing hardcoded colors yet. This task ONLY adds new variable definitions. The existing :root block, .panel-right/.panel-detail overrides, and all CSS rules remain untouched.**

**Step 1: Add `data-theme="mixed"` to the HTML element.**

Change line 2 from:
```html
<html lang="en">
```
to:
```html
<html lang="en" data-theme="mixed">
```

This ensures backward compatibility -- the app looks identical because "mixed" will match the current dark-root + light-panels behavior.

**Step 2: Replace the existing `:root` block (lines 9-41) with the two-layer token system.**

The new `:root` block contains ONLY palette primitives (Layer 1). These are static values that never change between themes. Extract every unique color value from the existing CSS into a named primitive.

Required palette primitives (organized by category):

```css
:root {
  /* LAYER 1: PALETTE PRIMITIVES */
  /* Raw color values. These NEVER change between themes. */

  /* Neutrals - dark side */
  --gray-950: #0f1117;
  --gray-900: #1a1d27;
  --gray-800: #242836;
  --gray-700: #2e3348;
  --gray-600: #8b8fa3;
  --gray-200: #e4e6f0;

  /* Neutrals - light side */
  --white: #ffffff;
  --cool-gray-50: #f5f6fa;
  --cool-gray-100: #eceef4;
  --cool-gray-150: #e2e5ee;
  --cool-gray-200: #d0d4e0;
  --cool-gray-800: #5c6070;
  --cool-gray-900: #1a1d27;

  /* Blues */
  --blue-400: #6c8cff;
  --blue-500: #4a5fa0;
  --blue-600: #2d5ee0;

  /* Status */
  --green-500: #4caf7c;
  --green-600: #16a34a;
  --orange-500: #e8a54c;
  --orange-600: #d97706;
  --red-500: #e05555;
  --red-600: #dc2626;
  --cyan-500: #5ec4d4;
  --cyan-600: #0891b2;
  --purple-500: #b07ee8;
  --purple-600: #7c3aed;

  /* Existing panel-right light values (for backward compat) */
  --light-surface: #f5f6f8;
  --light-surface2: #ecedf1;
  --light-surface3: #e2e4ea;
  --light-border: #d0d2da;
  --light-text: #1a1a2e;
  --light-text-dim: #6b6b80;
  --light-accent: #3b64e0;
  --light-accent-dim: #5a7ae8;
}
```

NOTE: The executor should audit ALL unique hex and rgba values in the CSS (lines 8-2463) during implementation to ensure every color that will be referenced gets a primitive. The list above covers the core values. Additional primitives will be needed for component-specific colors discovered during the audit (e.g., modal colors like `#4a4a60`, scrollbar colors like `#c0c0cc`, `#9090a0`, graph modal colors, etc.). Add them to the appropriate category in :root.

**Step 3: Add semantic token blocks for all three themes AFTER the :root block.**

Insert the following theme blocks immediately after the closing `}` of `:root`:

**Dark theme (default):**
```css
[data-theme="dark"] {
  /* Core */
  --bg: var(--gray-950);
  --surface: var(--gray-900);
  --surface2: var(--gray-800);
  --surface3: var(--gray-700);
  --border: var(--gray-700);
  --text: var(--gray-200);
  --text-dim: var(--gray-600);
  --accent: var(--blue-400);
  --accent-dim: var(--blue-500);

  /* Status */
  --green: var(--green-500);
  --orange: var(--orange-500);
  --red: var(--red-500);
  --cyan: var(--cyan-500);
  --purple: var(--purple-500);

  /* Highlights */
  --highlight: rgba(108,140,255,0.15);
  --highlight-confirmed: rgba(108,140,255,0.22);
  --highlight-preliminary: rgba(108,140,255,0.08);
  --highlight-rejected: rgba(224,85,85,0.08);

  /* Component: Modal (dark-themed modals in dark mode) */
  --modal-bg: var(--gray-900);
  --modal-text: var(--gray-200);
  --modal-text-dim: var(--gray-600);
  --modal-border: var(--gray-700);
  --modal-input-bg: var(--gray-800);
  --modal-input-border: var(--gray-700);

  /* Component: Tooltip */
  --tooltip-bg: var(--gray-900);
  --tooltip-border: var(--gray-700);
  --tooltip-text: var(--gray-200);

  /* Component: Graph (always dark) */
  --graph-header-bg: var(--gray-900);
  --graph-header-text: var(--gray-200);
  --graph-header-border: var(--gray-700);

  /* Scrollbar */
  --scrollbar-thumb: #555570;
  --scrollbar-thumb-hover: #6b6b8a;

  /* Confidence tiers */
  --conf-high: #4caf7c;
  --conf-high-bg: rgba(76,175,124,0.12);
  --conf-mid: #e8a54c;
  --conf-mid-bg: rgba(232,165,76,0.12);
  --conf-low: #e05555;
  --conf-low-bg: rgba(224,85,85,0.12);

  /* Feedback */
  --feedback-positive: #4caf7c;
  --feedback-positive-bg: rgba(76,175,124,0.12);
  --feedback-negative: #e05555;
  --feedback-negative-bg: rgba(224,85,85,0.12);

  /* Individuals */
  --individual-citation: var(--blue-400);
  --individual-entity: var(--cyan-500);

  /* Properties */
  --property-underline: var(--purple-500);

  /* Layer chips */
  --layer-concepts: var(--blue-400);
  --layer-individuals: var(--cyan-500);
  --layer-properties: var(--purple-500);
  --layer-pos: var(--orange-500);

  /* POS tag colors */
  --pos-noun: #5ec4d4;
  --pos-propn: #6c8cff;
  --pos-verb: #e05555;
  --pos-adj: #e8a54c;
  --pos-adv: #b07ee8;

  /* Misc */
  --undo-toast-bg: var(--gray-900);
  --badge-dot: var(--blue-400);

  /* Branch colors - dark theme (bright/vibrant for dark backgrounds) */
  --branch-actor-player: #5ec4d4;
  --branch-area-of-law: #e8a54c;
  --branch-asset-type: #f0c040;
  --branch-communication-modality: #b07ee8;
  --branch-currency: #f0c040;
  --branch-data-format: #8b8fa3;
  --branch-document-artifact: #e8a54c;
  --branch-document-metadata: #d4956a;
  --branch-engagement-terms: #4caf7c;
  --branch-event: #e05555;
  --branch-financial-concepts: #f0c040;
  --branch-folio-type: #e8a54c;
  --branch-forums-venues: #e887a0;
  --branch-governmental-body: #6c8cff;
  --branch-industry: #4caf7c;
  --branch-language: #e8a54c;
  --branch-legal-authorities: #e05555;
  --branch-legal-entity: #7bed9f;
  --branch-legal-use-cases: #b07ee8;
  --branch-location: #5ec4d4;
  --branch-matter-narrative: #b07ee8;
  --branch-matter-narrative-format: #5ec4d4;
  --branch-objectives: #e05555;
  --branch-service: #50c8ff;
  --branch-standards-compatibility: #8b8fa3;
  --branch-status: #e8a54c;
  --branch-system-identifiers: #8b8fa3;
  --branch-default: #8b8fa3;
}
```

**Light theme:**
```css
[data-theme="light"] {
  /* Core - per D-03 locked values */
  --bg: #ffffff;
  --surface: #f5f6fa;
  --surface2: #eceef4;
  --surface3: #e2e5ee;
  --border: #d0d4e0;
  --text: #1a1d27;
  --text-dim: #5c6070;
  --accent: #2d5ee0;  /* D-04: darker for WCAG compliance on white */
  --accent-dim: var(--blue-500);

  /* Status (darker variants for light backgrounds) */
  --green: var(--green-600);
  --orange: var(--orange-600);
  --red: var(--red-600);
  --cyan: var(--cyan-600);
  --purple: var(--purple-600);

  /* Highlights */
  --highlight: rgba(45,94,224,0.10);
  --highlight-confirmed: rgba(45,94,224,0.15);
  --highlight-preliminary: rgba(45,94,224,0.06);
  --highlight-rejected: rgba(220,38,38,0.06);

  /* Component: Modal */
  --modal-bg: #ffffff;
  --modal-text: #1a1d27;
  --modal-text-dim: #5c6070;
  --modal-border: #d0d4e0;
  --modal-input-bg: #f5f6fa;
  --modal-input-border: #d0d4e0;

  /* Component: Tooltip */
  --tooltip-bg: #ffffff;
  --tooltip-border: #d0d4e0;
  --tooltip-text: #1a1d27;

  /* Component: Graph (stays dark per open question recommendation) */
  --graph-header-bg: var(--gray-900);
  --graph-header-text: var(--gray-200);
  --graph-header-border: var(--gray-700);

  /* Scrollbar */
  --scrollbar-thumb: #c0c0cc;
  --scrollbar-thumb-hover: #9090a0;

  /* Confidence tiers */
  --conf-high: #16a34a;
  --conf-high-bg: rgba(22,163,74,0.10);
  --conf-mid: #d97706;
  --conf-mid-bg: rgba(217,119,6,0.10);
  --conf-low: #dc2626;
  --conf-low-bg: rgba(220,38,38,0.10);

  /* Feedback */
  --feedback-positive: #16a34a;
  --feedback-positive-bg: rgba(22,163,74,0.10);
  --feedback-negative: #dc2626;
  --feedback-negative-bg: rgba(220,38,38,0.10);

  /* Individuals */
  --individual-citation: var(--blue-600);
  --individual-entity: var(--cyan-600);

  /* Properties */
  --property-underline: var(--purple-600);

  /* Layer chips */
  --layer-concepts: var(--blue-600);
  --layer-individuals: var(--cyan-600);
  --layer-properties: var(--purple-600);
  --layer-pos: var(--orange-600);

  /* POS tag colors (darker for light backgrounds) */
  --pos-noun: #0e7490;
  --pos-propn: #2d5ee0;
  --pos-verb: #dc2626;
  --pos-adj: #d97706;
  --pos-adv: #7c3aed;

  /* Misc */
  --undo-toast-bg: var(--gray-900);  /* Toasts stay dark per research recommendation */
  --badge-dot: var(--blue-600);

  /* Branch colors - light theme (darker for WCAG on white, per D-05/D-06) */
  --branch-actor-player: #1e6fa0;
  --branch-area-of-law: #1a5276;
  --branch-asset-type: #6b5600;
  --branch-communication-modality: #7b4d93;
  --branch-currency: #7a5a00;
  --branch-data-format: #4a5568;
  --branch-document-artifact: #9c4a10;
  --branch-document-metadata: #8b5e3c;
  --branch-engagement-terms: #10613a;
  --branch-event: #b91c1c;
  --branch-financial-concepts: #6e4b00;
  --branch-folio-type: #6b5c00;
  --branch-forums-venues: #7b2d8e;
  --branch-governmental-body: #1a6091;
  --branch-industry: #065550;
  --branch-language: #7a3b10;
  --branch-legal-authorities: #8b1a1a;
  --branch-legal-entity: #085e40;
  --branch-legal-use-cases: #4a3570;
  --branch-location: #105560;
  --branch-matter-narrative: #6d3580;
  --branch-matter-narrative-format: #1a6894;
  --branch-objectives: #b03020;
  --branch-service: #065e4e;
  --branch-standards-compatibility: #4a5a6a;
  --branch-status: #864a08;
  --branch-system-identifiers: #3d4d5a;
  --branch-default: #5c6070;
}
```

**Mixed theme (dark root + light panels):**
```css
[data-theme="mixed"] {
  /* Root level: same as dark theme */
  --bg: var(--gray-950);
  --surface: var(--gray-900);
  --surface2: var(--gray-800);
  --surface3: var(--gray-700);
  --border: var(--gray-700);
  --text: var(--gray-200);
  --text-dim: var(--gray-600);
  --accent: var(--blue-400);
  --accent-dim: var(--blue-500);
  --green: var(--green-500);
  --orange: var(--orange-500);
  --red: var(--red-500);
  --cyan: var(--cyan-500);
  --purple: var(--purple-500);
  --highlight: rgba(108,140,255,0.15);
  --highlight-confirmed: rgba(108,140,255,0.22);
  --highlight-preliminary: rgba(108,140,255,0.08);
  --highlight-rejected: rgba(224,85,85,0.08);
  /* All other dark-theme tokens duplicated here */
  --modal-bg: var(--gray-900);
  --modal-text: var(--gray-200);
  --modal-text-dim: var(--gray-600);
  --modal-border: var(--gray-700);
  --modal-input-bg: var(--gray-800);
  --modal-input-border: var(--gray-700);
  --tooltip-bg: var(--gray-900);
  --tooltip-border: var(--gray-700);
  --tooltip-text: var(--gray-200);
  --graph-header-bg: var(--gray-900);
  --graph-header-text: var(--gray-200);
  --graph-header-border: var(--gray-700);
  --scrollbar-thumb: #555570;
  --scrollbar-thumb-hover: #6b6b8a;
  --conf-high: #4caf7c;
  --conf-high-bg: rgba(76,175,124,0.12);
  --conf-mid: #e8a54c;
  --conf-mid-bg: rgba(232,165,76,0.12);
  --conf-low: #e05555;
  --conf-low-bg: rgba(224,85,85,0.12);
  --feedback-positive: #4caf7c;
  --feedback-positive-bg: rgba(76,175,124,0.12);
  --feedback-negative: #e05555;
  --feedback-negative-bg: rgba(224,85,85,0.12);
  --individual-citation: var(--blue-400);
  --individual-entity: var(--cyan-500);
  --property-underline: var(--purple-500);
  --layer-concepts: var(--blue-400);
  --layer-individuals: var(--cyan-500);
  --layer-properties: var(--purple-500);
  --layer-pos: var(--orange-500);
  --pos-noun: #5ec4d4;
  --pos-propn: #6c8cff;
  --pos-verb: #e05555;
  --pos-adj: #e8a54c;
  --pos-adv: #b07ee8;
  --undo-toast-bg: var(--gray-900);
  --badge-dot: var(--blue-400);
  /* Dark-theme branch colors at root level */
  --branch-actor-player: #5ec4d4;
  --branch-area-of-law: #e8a54c;
  --branch-asset-type: #f0c040;
  --branch-communication-modality: #b07ee8;
  --branch-currency: #f0c040;
  --branch-data-format: #8b8fa3;
  --branch-document-artifact: #e8a54c;
  --branch-document-metadata: #d4956a;
  --branch-engagement-terms: #4caf7c;
  --branch-event: #e05555;
  --branch-financial-concepts: #f0c040;
  --branch-folio-type: #e8a54c;
  --branch-forums-venues: #e887a0;
  --branch-governmental-body: #6c8cff;
  --branch-industry: #4caf7c;
  --branch-language: #e8a54c;
  --branch-legal-authorities: #e05555;
  --branch-legal-entity: #7bed9f;
  --branch-legal-use-cases: #b07ee8;
  --branch-location: #5ec4d4;
  --branch-matter-narrative: #b07ee8;
  --branch-matter-narrative-format: #5ec4d4;
  --branch-objectives: #e05555;
  --branch-service: #50c8ff;
  --branch-standards-compatibility: #8b8fa3;
  --branch-status: #e8a54c;
  --branch-system-identifiers: #8b8fa3;
  --branch-default: #8b8fa3;
}

/* Mixed theme: light overrides for right and detail panels */
[data-theme="mixed"] .panel-right,
[data-theme="mixed"] .panel-detail {
  --bg: #ffffff;
  --surface: #f5f6fa;
  --surface2: #eceef4;
  --surface3: #e2e5ee;
  --border: #d0d4e0;
  --text: #1a1d27;
  --text-dim: #5c6070;
  --accent: #2d5ee0;
  --accent-dim: var(--blue-500);
  --green: var(--green-600);
  --orange: var(--orange-600);
  --red: var(--red-600);
  --cyan: var(--cyan-600);
  --purple: var(--purple-600);
  --highlight: rgba(45,94,224,0.10);
  --highlight-confirmed: rgba(45,94,224,0.15);
  --modal-bg: #ffffff;
  --modal-text: #1a1d27;
  --modal-text-dim: #5c6070;
  --modal-border: #d0d4e0;
  --modal-input-bg: #f5f6fa;
  --modal-input-border: #d0d4e0;
  --tooltip-bg: #ffffff;
  --tooltip-border: #d0d4e0;
  --tooltip-text: #1a1d27;
  --scrollbar-thumb: #c0c0cc;
  --scrollbar-thumb-hover: #9090a0;
  --conf-high: #16a34a;
  --conf-high-bg: rgba(22,163,74,0.10);
  --conf-mid: #d97706;
  --conf-mid-bg: rgba(217,119,6,0.10);
  --conf-low: #dc2626;
  --conf-low-bg: rgba(220,38,38,0.10);
  --feedback-positive: #16a34a;
  --feedback-positive-bg: rgba(22,163,74,0.10);
  --feedback-negative: #dc2626;
  --feedback-negative-bg: rgba(220,38,38,0.10);
  --individual-citation: var(--blue-600);
  --individual-entity: var(--cyan-600);
  --property-underline: var(--purple-600);
  --layer-concepts: var(--blue-600);
  --layer-individuals: var(--cyan-600);
  --layer-properties: var(--purple-600);
  --layer-pos: var(--orange-600);
  --pos-noun: #0e7490;
  --pos-propn: #2d5ee0;
  --pos-verb: #dc2626;
  --pos-adj: #d97706;
  --pos-adv: #7c3aed;
  --badge-dot: var(--blue-600);
  /* Light-theme branch colors for panels */
  --branch-actor-player: #1e6fa0;
  --branch-area-of-law: #1a5276;
  --branch-asset-type: #6b5600;
  --branch-communication-modality: #7b4d93;
  --branch-currency: #7a5a00;
  --branch-data-format: #4a5568;
  --branch-document-artifact: #9c4a10;
  --branch-document-metadata: #8b5e3c;
  --branch-engagement-terms: #10613a;
  --branch-event: #b91c1c;
  --branch-financial-concepts: #6e4b00;
  --branch-folio-type: #6b5c00;
  --branch-forums-venues: #7b2d8e;
  --branch-governmental-body: #1a6091;
  --branch-industry: #065550;
  --branch-language: #7a3b10;
  --branch-legal-authorities: #8b1a1a;
  --branch-legal-entity: #085e40;
  --branch-legal-use-cases: #4a3570;
  --branch-location: #105560;
  --branch-matter-narrative: #6d3580;
  --branch-matter-narrative-format: #1a6894;
  --branch-objectives: #b03020;
  --branch-service: #065e4e;
  --branch-standards-compatibility: #4a5a6a;
  --branch-status: #864a08;
  --branch-system-identifiers: #3d4d5a;
  --branch-default: #5c6070;
}
```

**Step 4: During implementation, the executor MUST audit the full CSS (lines 8-2463) for additional unique colors not yet covered by primitives.** Look for:
- Modal-specific colors (found around lines 2003-2070): `#4a4a60`, `#c0c0cc`, `#9090a0`, etc.
- Graph modal colors (lines 655-715): `#8b8fa3`, `#6c8cff`, etc.
- Scrollbar colors: `#c0c0cc`, `#9090a0`, `#555570`
- Detail section colors (line 391+): `#6b6b80`, `#e2e4ea`
- Any other hex/rgba not yet represented

Add any missing primitives to `:root` and corresponding semantic tokens to each theme block. The goal: every hardcoded color that Plan 02 will need to replace has a corresponding `var()` token ready.

**Naming conventions (per D-01, D-02):**
- Palette primitives: `--{hue}-{lightness}` (e.g., `--gray-900`, `--blue-400`)
- Semantic tokens: `--{purpose}` (e.g., `--bg`, `--surface`, `--modal-bg`)
- Branch colors: `--branch-{kebab-case-name}` (e.g., `--branch-actor-player`, `--branch-legal-entity`)
- All theme blocks MUST include the full set of semantic tokens -- no relying on cascade fallback from `:root`

**What NOT to do:**
- Do NOT remove or modify the existing `:root` variable definitions at this step -- replace the entire block with the new primitives-only `:root`
- Do NOT replace any hardcoded colors in CSS rules (that is Plan 02)
- Do NOT touch any JavaScript code
- Do NOT remove the existing `.panel-right` or `.panel-detail` override blocks (Plan 02 will handle that)
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-enrich" && grep -c 'data-theme="dark"' frontend/index.html && grep -c 'data-theme="light"' frontend/index.html && grep -c 'data-theme="mixed"' frontend/index.html && grep -c '\-\-branch-actor-player' frontend/index.html && grep 'data-theme="mixed"' frontend/index.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - frontend/index.html line 2 contains `data-theme="mixed"`
    - frontend/index.html :root block contains `--gray-950: #0f1117` (palette primitive)
    - frontend/index.html contains `[data-theme="dark"]` selector with `--bg: var(--gray-950)`
    - frontend/index.html contains `[data-theme="light"]` selector with `--bg: #ffffff`
    - frontend/index.html contains `[data-theme="mixed"]` selector with `--bg: var(--gray-950)`
    - frontend/index.html contains `[data-theme="mixed"] .panel-right` selector with `--bg: #ffffff`
    - frontend/index.html contains `--branch-actor-player` in at least 3 theme blocks (dark, light, mixed-panel)
    - frontend/index.html contains `--modal-bg` in at least 3 theme blocks
    - frontend/index.html contains `--tooltip-bg` in at least 3 theme blocks
    - Existing CSS rules (lines 43+) remain UNCHANGED (grep for a few known rules to confirm)
    - App loads without CSS errors when opened in browser (visual check)
  </acceptance_criteria>
  <done>Complete two-layer token system defined: palette primitives in :root, semantic tokens (including ~84 semantic + 27 branch tokens) in [data-theme="dark"], [data-theme="light"], [data-theme="mixed"], and [data-theme="mixed"] .panel-right/.panel-detail. HTML element has data-theme="mixed". No existing rules modified.</done>
</task>

</tasks>

<verification>
1. `grep -c 'data-theme=' frontend/index.html` returns multiple matches (at least 5: html element + 3 theme selectors + mixed panel selector)
2. `grep '\-\-gray-950' frontend/index.html` shows the primitive in :root
3. `grep '\-\-branch-actor-player' frontend/index.html` appears in dark, light, and mixed-panel blocks
4. `grep '\-\-modal-bg' frontend/index.html` appears in all theme blocks
5. Open frontend/index.html in browser -- app looks identical to before (data-theme="mixed" + existing rules = no visual change)
</verification>

<success_criteria>
- Two-layer token system is fully defined with palette primitives and semantic tokens
- All three theme selectors exist with complete variable sets
- Mixed theme has scoped panel overrides matching current light panel behavior
- 26 branch colors defined per theme (78 total + default)
- HTML element has data-theme="mixed"
- Application visual appearance is completely unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-variable-foundation/01-01-SUMMARY.md`
</output>
