---
phase: 03
slug: consolidated-system-status-chip
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-22
---

# Phase 03 — UI Design Contract

> Visual and interaction contract for the consolidated "System" status chip + anchored popover.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Implements locked decisions D-01..D-12 from `03-CONTEXT.md`. Do not re-litigate those decisions.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (single-file vanilla-JS frontend `frontend/index.html`, inline `<style>`, no build step, no new dependencies) |
| Preset | not applicable |
| Component library | none — extend existing `.status-chip` / `.folio-status-row` patterns |
| Icon library | none — inline SVG glyphs only (matches Phase 02 vendored-SVG precedent; no icon font, no emoji, must survive content blockers) |
| Font | existing system stack: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif` (`index.html:502`). Do NOT introduce a new font. |

**Hard constraint:** This contract reuses the v1.0 CSS-variable token system across the three themes (Dark / Light / Mixed, selected via `data-theme` on `<html>`). Every color below is an existing CSS variable. Do NOT propose a new design system, framework, or token set. New CSS lives in the same inline `<style>` block; new markup replaces the four chips at `index.html:2960-2980` (`chipLLM` at `:2981-2986` stays untouched per STATUS-06 / D-06 scope).

---

## Spacing Scale

The host stylesheet uses a 4-based scale already (`gap:4px`, `padding:3px 10px`, `gap:8px`, `margin-bottom:16px`). Match it. Declared values (multiples of 4):

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Icon-to-label gap in collapsed chip; gap between status icon and row label in popover |
| sm | 8px | Gap between status icon and text inside a popover row; popover internal column gap |
| md | 12px | Popover row vertical padding (`10–12px`); horizontal row padding `12–14px` (mirrors `.folio-status-row` `10px 14px`) |
| lg | 16px | Popover content block separation (between rows is via border, not margin); popover internal top/bottom padding |
| — | 6px | Popover corner radius (`border-radius`), matching modal cards (`.folio-status-row` uses 8px; popover uses 6–8px) |

**Exceptions (required):**
- Collapsed chip preserves the existing `.status-chip` box metrics exactly: `padding: 3px 10px; border-radius: 4px; gap: 5px; font-size: 11px`. Do NOT enlarge the chip — header density must be unchanged so D-12 (overlap relief from removing 3 chips) holds.
- Popover anchors **4px below** the chip's bottom edge (small visual gap), top-aligned to the chip's left edge, using `position: fixed` like the existing `data-tooltip` (`top:44px` reference at `index.html:570-583`) to avoid header reflow (D-01).
- Status-icon hit area inside the collapsed chip is the whole chip (chip is the button); the chip's existing height is the touch target. No separate 44px target is introduced (desktop header; mobile is deferred).

---

## Typography

Reuse the header/modal type ramp already in the file. Two weights only (regular 400 + semibold/600); the chip uses 500 for its label exactly as `.chip-label` does today.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Chip label ("System" / "System: FOLIO +1") | 11px | 500 | 1 (single line, `white-space: nowrap`) |
| Popover row subsystem name | 13px | 600 | 1.3 |
| Popover row status word + metrics | 12px | 400 | 1.4 |
| Popover row "Standby / Update" annotation + "Manage" link | 11px | 500 | 1.3 |

No display/heading tier — the popover has no title bar (it is a disclosure region, not a modal). If a header label is wanted, use a single 11px uppercase eyebrow ("System status", `color: var(--text-dim)`, `letter-spacing: 0.5px`) mirroring `.folio-stat-label` at `index.html:1305`.

---

## Color

Reuse existing semantic tokens. The chip and popover are surface-on-surface; accent and red are reserved for the specific elements listed.

| Role | Value (CSS var) | Usage |
|------|-----------------|-------|
| Dominant (60%) | `var(--surface2)` (Dark `#242836` / Light `#eceef4`) | Collapsed chip background (unchanged from `.status-chip`) |
| Secondary (30%) | `var(--surface3)` (Dark `#2e3348` / Light `#e2e5ee`) | Popover panel background; row hover background |
| Accent (10%) | `var(--accent)` (Dark `#6c8cff` / Light `#2d5ee0`) | ONLY: keyboard focus ring on chip + popover + "Manage" action; chip hover border; the "Manage" text link in the FOLIO row |
| Status — healthy | `var(--green)` (Dark `#4caf7c` / Light `#16a34a`) | Healthy status icon (check) fill, in chip + popover rows |
| Status — warning | `var(--orange)` (Dark `#e8a54c` / Light `#d97706`) | Warning icon fill — RESERVED for the future partial-degradation tier (D-07); no current subsystem state maps here |
| Status — error / Destructive | `var(--red)` (Dark `#e05555` / Light `#dc2626`) | Error/offline icon fill in chip + popover rows; the only "alarming" color |
| Text | `var(--text)` | Chip label, subsystem names |
| Text-dim | `var(--text-dim)` | Metrics, Standby/Update annotations, eyebrow |
| Borders | `var(--border)` | Chip border, popover border, row dividers |

**Accent reserved for:** (1) focus-visible ring on the System chip, the popover container, and the FOLIO "Manage" action; (2) chip `:hover`/`:focus` border-color (existing `.status-chip:hover` → `--accent-dim`); (3) the "Manage" inline link affordance in the FOLIO popover row. Accent is NEVER used as a status color and NEVER applied to non-interactive text.

**Status-color quiet-green mapping (D-05/D-06/D-07 — locked):**
- **Green (healthy / quiet at rest):** all subsystems ready, OR FOLIO/Embedding `Standby` (not_loaded), OR FOLIO `Update Available` / `Updating…`. Fresh page load = green.
- **Orange (warning):** reserved tier, currently unused. Keep the icon + token available.
- **Red (error):** any subsystem errored/offline — Backend offline, FOLIO error, Embedding error, spaCy error.
- Rollup = worst of the four: **red > orange > green**.

**WCAG AA (mandatory, enforced by `scripts/contrast-audit.mjs`; body 4.5:1, large/icon 3:1, target margin 5.5:1):**
- Status is conveyed by **icon shape + text**, never color alone (STATUS-05 / D-09). Color is redundant reinforcement.
- Status icons are non-text graphical objects → require ≥ 3:1 against their background (`--surface2` chip / `--surface3` popover). Existing `--green`/`--orange`/`--red` already pass on these surfaces per the v1.0 audit; if any icon fails 3:1, render the glyph stroke at `var(--text)` and keep color as a secondary fill so the shape still reads.
- All popover text (subsystem name, metrics, annotations) must hit 4.5:1 on `--surface3` in all three themes. Run `node scripts/contrast-audit.mjs` after implementation; zero FAILs required.

---

## Iconography (status glyphs — D-09)

Inline SVG, `currentColor`-driven where possible, ~12px in the chip, ~14px in popover rows. Shapes must be distinguishable without color:

| State | Glyph | Shape rationale | Color |
|-------|-------|-----------------|-------|
| Healthy | **check** inside a circle (or bare check) | rounded, "all good" | `var(--green)` |
| Warning | **exclamation in a triangle** | angular triangle = caution (reserved tier) | `var(--orange)` |
| Error | **× (cross)** in a circle, or filled octagon-style | distinct cross / stop shape | `var(--red)` |

The three shapes (round-check / triangle-bang / cross-circle) are deliberately different silhouettes so a colorblind or grayscale user distinguishes them by form. The collapsed chip shows the **rollup** glyph; each popover row shows its **own** glyph.

---

## Component Layout Contract

### Collapsed chip (replaces `chipBackend`/`chipFolio`/`chipEmbedding`/`chipSpacy`)
- Single `.status-chip` reusing existing box styling, now `role="button"`, `tabindex="0"`, `aria-haspopup="true"`, `aria-expanded="false|true"`, `aria-controls="systemStatusPopover"`.
- Content order: `[status icon] [label]`. Healthy → icon + **"System"** (D-10). Degraded → worst icon + **"System: {Subsystem}"**, with **"+N"** overflow when >1 fails, e.g. **"System: FOLIO +1"** (D-11).
- No gear on the aggregate chip (D-08 — gear/manage moves into the FOLIO row).
- Existing `data-tooltip` may carry a one-line summary, but the popover is the source of truth; tooltip is optional reinforcement.

### Anchored popover (`#systemStatusPopover`, D-01/D-02/D-03/D-04)
- `position: fixed`, dropped 4px below the chip, left-aligned to chip; `background: var(--surface3)`; `1px solid var(--border)`; `border-radius: 6–8px`; subtle shadow consistent with existing modals; `z-index` above header (≥ 100, like the tooltip).
- `role="region"` (or `role="dialog"` if focus is trapped) with `aria-label="System status detail"`.
- Always available (never hidden when green) — D-02. Shows **all four rows** at all times.
- One row per subsystem in fixed order: **Backend, FOLIO, Embedding, spaCy**. Each row mirrors `.folio-status-row` rhythm (`display:flex; align-items:center; gap:8px; padding:10px 14px`), separated by `1px var(--border)` dividers (not margins, to keep the panel compact).
- Row anatomy: `[status icon] [subsystem name (13px/600)] … [status word + metric (12px/400, --text-dim)]`. Standby/Update get a trailing 11px annotation pill/text in `--text-dim` (e.g. "Standby — loads on first use", "Update available").
- **FOLIO row only:** a right-aligned **"Manage"** action (accent text link or small ghost button), `aria-label="Manage FOLIO ontology"`, calling existing `openFolioModal()` unchanged (D-08). No other row has an action.
- Rows update live while open from the existing health poll (D-03): the collapsed chip rollup and the matching row re-render in place when a poll arrives (e.g. FOLIO Standby → ready).

### Metrics to preserve verbatim (STATUS-04 — from `/health/detail`)
- **Backend:** running / "Offline".
- **FOLIO:** `{concepts} concepts, {labels_indexed} labels indexed`; Standby; "Update Available" / "Updating…".
- **Embedding:** `{provider}, {index_size} vectors indexed`; Standby.
- **spaCy:** `spaCy {version} — EntityRuler ready`.
Consolidation must not drop any metric currently shown via `setChip()` tooltips (`index.html:4038-4130`).

### Header overlap (STATUS-07 / D-12)
- Four chips → one frees header width; the System chip occupies the former status-chip area, `chipLLM` stays to its right. Confirm in planning that `#statusBar` no longer overlaps `#layerToggleBar`. If residual overlap remains, prefer `flex-wrap`/`min-width:0` on `.status-bar` over shrinking the chip.

---

## Interaction & Accessibility Contract (D-04)

| Concern | Contract |
|---------|----------|
| Open | Click on chip, or focus chip + Enter/Space (reuse existing `.status-chip.clickable` keydown handler at `index.html:10404`). Sets `aria-expanded="true"`. |
| Close | Escape (extend the existing Escape handler at `index.html:10389`), outside-click, or re-activating the chip. Sets `aria-expanded="false"`. |
| Focus on open | Move focus into the popover (first focusable = the popover container or the FOLIO "Manage" action). |
| Focus on close | Restore focus to the System chip. |
| Focus-visible | Visible accent ring (`var(--accent)`) on chip, popover, and "Manage" — never `outline:none` without a replacement (matches existing `textarea:focus` accent pattern at `index.html:1267`). |
| Screen reader | Each row announces "{Subsystem}: {status word}, {metric}". Chip announces its label ("System" / "System: FOLIO +1") and expanded state. Status conveyed by icon + text, not color (STATUS-05). |
| Live updates | When the popover is open and a poll changes a row, update text in place; avoid an `aria-live` storm — prefer `aria-live="off"` on rows and let the user re-read, OR a single polite live region only for status-tier changes. (Implementer's discretion within "no announcement spam".) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (FOLIO row action) | **"Manage"** (aria-label: "Manage FOLIO ontology") |
| Collapsed chip — healthy | **"System"** |
| Collapsed chip — single failure | **"System: {Subsystem}"** (e.g. "System: FOLIO", "System: Backend") |
| Collapsed chip — multiple failures | **"System: {WorstSubsystem} +N"** (e.g. "System: Backend +2") |
| Popover row — Backend ready | "Running" |
| Popover row — Backend offline | "Offline — cannot reach backend" |
| Popover row — FOLIO ready | "{n} concepts, {n} labels indexed" |
| Popover row — FOLIO standby | "Standby — loads on first use" |
| Popover row — FOLIO update available | "Update available" (+ existing update note) |
| Popover row — FOLIO updating | "Updating…" |
| Popover row — Embedding ready | "{provider}, {n} vectors indexed" |
| Popover row — Embedding standby | "Standby — loads on first use" |
| Popover row — spaCy ready | "spaCy {version} — EntityRuler ready" |
| Error state (any subsystem) | "{Subsystem} error" + existing backend `message` if present (e.g. "FOLIO error — failed to load ontology") |
| Empty/loading state | While first poll is pending, chip shows neutral icon + "System" with a checking tooltip; rows read "Checking…" (mirrors current "Checking..." / "..." placeholders at `index.html:2960-2979`). |
| Destructive confirmation | None — this phase has no destructive actions. "Manage" opens the existing read/manage FOLIO modal unchanged; no delete/reset is introduced. |

**Tone:** terse, status-bar register, sentence-case, no emoji. "Standby — loads on first use" must appear so the quiet-green nuance is visible (D-05). Informational states (Standby, Update available) are phrased neutrally, never as warnings (D-06).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no component registry; no third-party blocks; no new dependencies (single-file vanilla JS) |

shadcn gate: not applicable — project is not React/Next.js/Vite and ships no `components.json`. Icons are hand-authored inline SVG, not pulled from any registry.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
