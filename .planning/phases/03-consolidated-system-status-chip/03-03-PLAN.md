---
phase: 03-consolidated-system-status-chip
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/index.html
autonomous: true
requirements: [STATUS-01, STATUS-05, STATUS-07]
must_haves:
  truths:
    - "The header shows a single System disclosure chip in place of the four Backend/FOLIO/Embedding/spaCy chips; the LLM chip remains separate and untouched"
    - "The System chip is a role=button disclosure with aria-expanded, aria-controls, tabindex, aria-haspopup"
    - "An anchored position:fixed popover region exists immediately after the chip in source order with four subsystem rows in fixed order"
    - "Status icons are inline SVG with three distinct silhouettes (check / triangle / cross), stroked at --text so they clear 3:1 in every theme"
    - "The System chip occupies the former four-chip area; the freed width is expected to resolve the layer-chip overlap (visual confirmation in plan 04 Task 3 UAT)"
    - "An exported-free byte-identical copy of normalizeSubsystems/computeRollup/chipLabel lives inline in index.html"
  artifacts:
    - path: "frontend/index.html"
      provides: "System disclosure chip + popover markup, status-icon SVGs + CSS, inline rollup module copy"
      contains: "systemStatusPopover"
  key_links:
    - from: "frontend/index.html #chipSystem"
      to: "frontend/index.html #systemStatusPopover"
      via: "aria-controls"
      pattern: "aria-controls=\"systemStatusPopover\""
    - from: "frontend/index.html inline rollup copy"
      to: "scripts/system-rollup.mjs"
      via: "byte-identical sync (Phase 02 flags.mjs precedent)"
      pattern: "keep byte-identical"
---

<objective>
Replace the four passive status chips (Backend/FOLIO/Embedding/spaCy) with one accessible "System" disclosure chip plus a four-row anchored popover, add the inline-SVG status glyphs and theme-aware CSS (with the mandatory `--text` stroke fallback so Light-theme green/orange clear 3:1), and paste the byte-identical inline copy of the rollup module. This plan does the MARKUP + CSS + inline-module-copy; Wave 3 (plan 04) wires `checkHealth()` and the disclosure behavior. The LLM chip and its code path are a hard no-touch boundary (STATUS-06).

Purpose: Establish the consolidated chip/popover structure and styling so the next plan can drive it from health data without inventing structure.
Output: `frontend/index.html` with the System chip + popover markup, status-icon SVGs + CSS, the inline byte-identical rollup module, and the old four chips removed. STATUS-07 header overlap resolved/verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md
@.planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md
@.planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md
@.planning/phases/03-consolidated-system-status-chip/03-01-SUMMARY.md
@scripts/system-rollup.mjs

<interfaces>
<!-- Disclosure chip + popover markup to adapt (RESEARCH lines 173-187), placed where the 4 chips were. -->
  <div class="status-chip system-chip clickable" id="chipSystem"
       role="button" tabindex="0" aria-haspopup="true" aria-expanded="false"
       aria-controls="systemStatusPopover" aria-label="System status">
    <span class="chip-status-icon"><!-- inline SVG rollup glyph --></span>
    <span class="chip-label" id="chipSystemLabel">System</span>
  </div>
  <div class="system-popover" id="systemStatusPopover" role="region"
       aria-label="System status detail" tabindex="-1" hidden>
    <!-- 4 rows: Backend, FOLIO, Embedding, spaCy (fixed order). FOLIO row has a "Manage FOLIO" action. -->
  </div>

<!-- Existing markup to replace (frontend/index.html:2959-2987). KEEP chipLLM (:2981-2986) byte-for-byte. -->
  #statusBar > [chipBackend :2960-2964] [chipFolio :2965-2970, has onclick=openFolioModal()+gear]
              [chipEmbedding :2971-2975] [chipSpacy :2976-2980] [chipLLM :2981-2986 KEEP]

<!-- CSS anchors to reuse (do NOT invent): -->
  .status-chip                      :536-553  (padding:3px 10px; border-radius:4px; gap:5px; font-size:11px; background:var(--surface2); border:1px solid var(--border); color:var(--text-dim); white-space:nowrap)
  .status-chip:hover                (border-color:var(--accent-dim))
  .status-chip[data-tooltip]:hover::after :570-583  (position:fixed; top:44px; background:var(--surface3); z-index:100) — popover positioning analog
  .status-chip.clickable / .chip-gear     :1270-1280
  .folio-status-row / .folio-status-dot   :1284-1296  (display:flex; align-items:center; gap:8px; padding:10px 14px) — per-row analog
  .chip-dot color classes           :554-564  (being replaced by status-icon SVGs)

<!-- Inline rollup-module byte-identical copy goes near the existing FLAG_SVG block: -->
  frontend/index.html:10297  // ── Translation flags (... mirrors scripts/flags.mjs — keep byte-identical) ──

<!-- Status-icon SVGs (RESEARCH lines 322-350): check-in-circle / exclamation-triangle / cross-in-circle.
     aria-hidden="true" focusable="false". Stroke at --text (≥13:1 Light) carries 3:1; status color as secondary fill. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace four chips with System disclosure chip + popover; re-home FOLIO Manage</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html lines 2955-2995 (the four chips to remove, chipLLM to KEEP, layer-toggle-bar that overlaps)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 173-187 (disclosure chip + popover markup)
    - .planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md lines 111-139 (Component Layout Contract — chip + popover + FOLIO Manage action + header overlap)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 157-173 (markup edit cluster (a))
  </read_first>
  <action>In frontend/index.html #statusBar, remove the four chip divs `chipBackend` (:2960-2964), `chipFolio` (:2965-2970), `chipEmbedding` (:2971-2975), `chipSpacy` (:2976-2980) and their `*Detail` spans. Leave `chipLLM` (:2981-2986), its `onclick="onLLMChipClick()"`, gear, and `chipLLMDetail` byte-for-byte intact (STATUS-06). Insert the System disclosure chip `#chipSystem` (class `status-chip system-chip clickable`, `role="button"`, `tabindex="0"`, `aria-haspopup="true"`, `aria-expanded="false"`, `aria-controls="systemStatusPopover"`, `aria-label="System status"`) with a `.chip-status-icon` span (placeholder for the rollup SVG) and a `.chip-label` span `#chipSystemLabel` reading "System". Immediately AFTER the chip in source order, insert the `#systemStatusPopover` region (`role="region"`, `aria-label="System status detail"`, `tabindex="-1"`, `hidden`) containing four rows in fixed order Backend, FOLIO, Embedding, spaCy. Each row: a status-icon slot, a 13px/600 subsystem name, a 12px/400 status+metric slot, and a trailing 11px annotation slot. The FOLIO row ONLY gets a right-aligned "Manage FOLIO" action (accent text link or ghost button, `aria-label="Manage FOLIO ontology"`, `onclick="openFolioModal()"`) re-homing the old FOLIO chip affordance per D-08 — do NOT add an action to any other row, do NOT put a gear on #chipSystem (D-08). Use placeholder text ("Checking…" / "…") for row content matching the current "Checking..." placeholder convention; plan 04 fills it live.</action>
  <verify>
    <automated>grep -c 'id="chipBackend"\|id="chipFolio"\|id="chipEmbedding"\|id="chipSpacy"' frontend/index.html; grep -c 'id="chipSystem"\|id="systemStatusPopover"\|id="chipLLM"' frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - source: `grep -c 'id="chipBackend"' frontend/index.html` returns 0; same for chipFolio, chipEmbedding, chipSpacy
    - source: frontend/index.html contains `id="chipSystem"` with `role="button"`, `aria-expanded="false"`, `aria-controls="systemStatusPopover"`
    - source: frontend/index.html contains `id="systemStatusPopover"` with `role="region"` and `hidden`, placed after #chipSystem
    - source: frontend/index.html still contains `id="chipLLM"` and `onclick="onLLMChipClick()"` unchanged (STATUS-06)
    - source: the popover FOLIO row contains `openFolioModal()` and `aria-label="Manage FOLIO ontology"`; no other row has an action; #chipSystem has no `chip-gear`
    - behavior: the popover has exactly four rows in order Backend, FOLIO, Embedding, spaCy
  </acceptance_criteria>
  <done>Four chips replaced by one System disclosure chip + a four-row popover with the FOLIO Manage action re-homed; LLM chip untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add status-icon SVGs + chip/popover/icon CSS with --text stroke fallback</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html lines 536-584 (.status-chip + data-tooltip fixed-position pattern), lines 1267-1296 (.status-chip.clickable focus/hover + .folio-status-row rhythm)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 322-350 (the three inline-SVG glyphs + stroke-fallback note)
    - .planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md lines 67-139 (Color, Iconography, Component Layout Contract — exact tokens + box metrics)
    - .planning/phases/03-consolidated-system-status-chip/03-02-SUMMARY.md (which status-icon pairs FAIL as solid fills → must be stroked at --text)
  </read_first>
  <action>In the inline `<style>` block of frontend/index.html, add CSS for `.system-chip`/`#chipSystem`, `.chip-status-icon`, `.system-popover`/`#systemStatusPopover`, and the per-row classes. Reuse existing tokens ONLY — no hex literals in component CSS. Preserve the collapsed-chip box metrics verbatim (`padding:3px 10px`, `border-radius:4px`, `gap:5px`, `font-size:11px`) so header density is unchanged (D-12). Popover: `position:fixed` dropped 4px below the chip, left-aligned, `background:var(--surface3)`, `1px solid var(--border)`, `border-radius:6px`, subtle shadow, `z-index:100` (mirror the data-tooltip fixed pattern at :570-583). Rows: `display:flex; align-items:center; gap:8px; padding:10px 14px` separated by `1px solid var(--border)` dividers (mirror .folio-status-row at :1284, swap surface to --surface3). Add `:focus-visible` accent rings (`var(--accent)`) on #chipSystem, the popover, and the Manage action (never bare `outline:none`). Author the three inline-SVG status glyphs — check-in-circle (healthy), exclamation-triangle (warning, reserved tier), cross-in-circle (error) — from RESEARCH lines 322-350, each `aria-hidden="true" focusable="false"`, ~12px in the chip / ~14px in rows, with three distinct silhouettes readable in grayscale. Stroke every glyph at `var(--text)` (the guaranteed ≥3:1 outline in all themes per the 02-SUMMARY findings) and apply the status color (`var(--green)`/`var(--orange)`/`var(--red)`) as a secondary fill — color is redundant reinforcement, never the sole signal (STATUS-05/D-09). Provide CSS state classes (e.g. `.is-green`/`.is-orange`/`.is-red`) so plan 04 can toggle the rollup glyph's status color.</action>
  <verify>
    <automated>node scripts/contrast-audit.mjs && echo "AUDIT PASS"</automated>
  </verify>
  <acceptance_criteria>
    - source: frontend/index.html CSS for `#systemStatusPopover` uses `position:fixed` and `var(--surface3)`; no hex literals in the new component CSS
    - source: the three status-icon SVGs are inline `<svg>` with `aria-hidden="true"` and `focusable="false"`, stroked at `var(--text)` (or currentColor driven to --text)
    - source: collapsed chip CSS keeps `padding:3px 10px`, `gap:5px`, `border-radius:4px`, `font-size:11px`
    - source: `:focus-visible` accent ring present for #chipSystem and the Manage action; no bare `outline:none` without a replacement
    - behavior: `node scripts/contrast-audit.mjs` exits 0 with zero FAILs including the status-icon green/orange/red on --surface2/--surface3 checks in all three themes (proves the --text stroke clears 3:1 — STATUS-05)
  </acceptance_criteria>
  <done>Three distinct inline-SVG status glyphs and theme-aware chip/popover/row CSS added; the extended contrast audit passes with zero FAILs in all themes.</done>
</task>

<task type="auto">
  <name>Task 3: Paste byte-identical inline rollup module copy</name>
  <files>frontend/index.html</files>
  <read_first>
    - scripts/system-rollup.mjs (the exported authoritative source to mirror)
    - frontend/index.html lines 10297-10354 (the existing FLAG_SVG byte-identical inline block + its sync header — the exact precedent)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 23-51, 271-274 (byte-identical sibling pattern + edit cluster (e))
  </read_first>
  <action>Into the `<script>` block of frontend/index.html, near the existing inline FLAG_SVG block (:10297), paste the bodies of `TIER_RANK`, `normalizeSubsystems`, `computeRollup`, and `chipLabel` from scripts/system-rollup.mjs — with the `export` keyword REMOVED from each (the inline copy must not use ES `import`/`export`; do NOT add a real `<script type="module">` import of system-rollup.mjs). Prepend a sync header comment exactly in the style of the flags block: `// ── System status rollup (mirrors scripts/system-rollup.mjs — keep byte-identical) ──`. The function bodies must be byte-identical to the .mjs (only the `export` prefixes differ), so the unit tests in scripts/system-rollup.test.mjs stay authoritative for the inline copy. Do NOT call these functions yet — plan 04 wires them into checkHealth().</action>
  <verify>
    <automated>grep -c "keep byte-identical" frontend/index.html; grep -c "function computeRollup\|function normalizeSubsystems\|function chipLabel" frontend/index.html; node --test scripts/system-rollup.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - source: frontend/index.html contains the comment "System status rollup (mirrors scripts/system-rollup.mjs — keep byte-identical)"
    - source: frontend/index.html contains `function normalizeSubsystems`, `function computeRollup`, `function chipLabel` WITHOUT the `export` keyword on those inline definitions
    - source: frontend/index.html contains no `import` of `system-rollup.mjs` (no ES module import into the single-file frontend)
    - behavior: `node --test scripts/system-rollup.test.mjs` still exits 0 (the authoritative .mjs is unchanged)
    - the inline function bodies match scripts/system-rollup.mjs line-for-line except for the dropped `export` prefixes
  </acceptance_criteria>
  <done>A byte-identical, export-free inline copy of the rollup module lives in index.html under a sync header, mirroring the Phase 02 flags precedent; no ES import added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend `/health/detail` strings → DOM | The popover will render backend-supplied subsystem strings. This plan only creates static markup/placeholders; live rendering (with textContent) is plan 04. No interpolated backend strings enter the DOM here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering/Info-disclosure | popover row markup | accept (this plan) | Static placeholder markup only; DOM-XSS mitigation (textContent, never innerHTML of message strings) enforced in plan 04 where strings are injected. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs (single-file vanilla JS, no build step). Any install task contradicts locked scope and must be rejected. |

No HIGH-severity threats: static markup/CSS edits and an inline pure-logic copy; no untrusted input rendered in this plan.
</threat_model>

<verification>
- `node scripts/contrast-audit.mjs` exits 0 with zero FAILs (including the status-icon 3:1 checks across Dark/Light/Mixed — STATUS-05).
- `grep` confirms the four old chip IDs are gone and #chipSystem + #systemStatusPopover exist; #chipLLM unchanged (STATUS-06).
- `node --test scripts/system-rollup.test.mjs` still passes (inline copy did not alter the .mjs).
- Manual UAT (Chrome DevTools MCP, deferred to plan 04 / verify-work): #statusBar no longer overlaps #layerToggleBar after a document loads (STATUS-07).
</verification>

<success_criteria>
- STATUS-01: a single System chip replaces the four subsystem chips (markup); LLM chip remains separate (STATUS-06).
- STATUS-05: three distinct inline-SVG silhouettes stroked at --text clear 3:1 in every theme (audit-proven).
- STATUS-07: collapsed chip preserves the existing footprint so freed width relieves the layer-chip overlap (CSS; final visual confirmation in UAT).
- The inline rollup copy is byte-identical to the tested .mjs.
</success_criteria>

<output>
Create `.planning/phases/03-consolidated-system-status-chip/03-03-SUMMARY.md` when done. Record the final popover row DOM structure (element IDs/classes per row, the status-color state-class names, the icon slot selector) so plan 04 can target nodes for in-place live updates without rebuilding the DOM.
</output>
