---
phase: 03-consolidated-system-status-chip
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/system-rollup.mjs
  - scripts/system-rollup.test.mjs
autonomous: true
requirements: [STATUS-02, STATUS-03]
must_haves:
  truths:
    - "All-healthy subsystems roll up to a single green 'System' label"
    - "Any errored subsystem rolls the chip to red and names the failing subsystem"
    - "Multiple failures show worst subsystem plus a +N overflow count"
    - "FOLIO/Embedding Standby (not_loaded) and FOLIO Update flags normalize to green tier, not orange"
  artifacts:
    - path: "scripts/system-rollup.mjs"
      provides: "Pure exported normalizeSubsystems / computeRollup / chipLabel"
      contains: "export function computeRollup"
    - path: "scripts/system-rollup.test.mjs"
      provides: "Node built-in test coverage for STATUS-02/STATUS-03 and D-05/D-06"
      contains: "node:test"
  key_links:
    - from: "scripts/system-rollup.test.mjs"
      to: "scripts/system-rollup.mjs"
      via: "ES import of exported functions"
      pattern: "from './system-rollup.mjs'"
---

<objective>
Create the pure, exported, unit-tested status-rollup logic module that drives the consolidated System chip. This is the single source of truth for the worst-of-four rollup (STATUS-03) and the quiet-green-at-rest mapping (STATUS-02 / D-05 / D-06). Wave 2 will paste a byte-identical NON-export copy into `frontend/index.html` (Phase 02 `scripts/flags.mjs` precedent); this plan owns the testable authoritative copy.

Purpose: Make the locked D-05/D-06/D-07 mapping unit-testable in a no-build single-file frontend, so the rollup logic cannot silently drift or regress.
Output: `scripts/system-rollup.mjs` (exported pure functions) + `scripts/system-rollup.test.mjs` (passing Node built-in tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md
@.planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md
@.planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md

<interfaces>
<!-- Authoritative analog: scripts/flags.mjs (exported) ↔ scripts/flags.test.mjs (imports it). Mirror its shape EXACTLY. -->

From scripts/flags.mjs (export style to copy):
  export const FLAG_SVG = { ... };
  export function localeToCountry(locale) { ... }
  export function localeLabel(locale) { ... }
  export function flagMarkup(locale) { ... }
  // top-level `export const` / `export function`, NO default export, NO imports beyond node: built-ins

From scripts/flags.test.mjs (test framework to copy):
  import { test } from 'node:test';
  import assert from 'node:assert/strict';
  import { ... } from './flags.mjs';

Backend /health/detail response shape (read-only, verified backend/app/api/routes/health.py — DO NOT change backend):
  folio_ontology: { status: "ready"|"not_loaded"|"error", concepts, labels_indexed,
                    message?, update_status: { update_in_progress, update_available,
                                               last_update_at, concepts_before, concepts_after } }
  embedding:      { status: "ready"|"not_loaded"|"error", provider, index_size, message? }
  spacy:          { status: "ready"|"error", version, message? }
  llm:            { ... }   // EXCLUDED from rollup (STATUS-06) — normalizeSubsystems must NOT read d.llm

Rollup contract (from 03-RESEARCH.md § Pattern 3, lines 229-251):
  TIER_RANK = { green: 0, orange: 1, red: 2 }
  computeRollup(subsystems) -> { tier, worstName, overflow }   // overflow = max(0, failCount-1)
  chipLabel(rollup) -> "System" (green) | "System: {worstName}" | "System: {worstName} +{overflow}"
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write failing tests for computeRollup + chipLabel (STATUS-02/STATUS-03)</name>
  <files>scripts/system-rollup.test.mjs</files>
  <read_first>
    - scripts/flags.test.mjs (test framework + naming + requirement-tagging precedent to mirror exactly)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 229-251 (Pattern 3 — computeRollup/chipLabel contract)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 106-149 (system-rollup.test.mjs analog + sample tests)
  </read_first>
  <behavior>
    - STATUS-02: computeRollup of four green subsystems returns { tier: 'green', worstName: null, overflow: 0 }; chipLabel returns exactly "System".
    - STATUS-03: computeRollup with one red subsystem returns tier 'red' with worstName set to that subsystem; chipLabel returns "System: {Name}".
    - STATUS-03 overflow: two red subsystems → chipLabel returns "System: {WorstName} +1" (overflow = failCount - 1).
    - worst-of-four ranking: a single red among greens picks red as worst (red > orange > green via TIER_RANK).
    - overflow counts only the OTHER non-green subsystems beyond the worst (3 reds → "+2").
  </behavior>
  <action>Create scripts/system-rollup.test.mjs importing `{ TIER_RANK, normalizeSubsystems, computeRollup, chipLabel } from './system-rollup.mjs'`. Use `import { test } from 'node:test'` and `import assert from 'node:assert/strict'` — copy the framework lines verbatim from scripts/flags.test.mjs. Write the computeRollup/chipLabel tests described in <behavior>, with section-banner comments and STATUS-02/STATUS-03 IDs in the test titles (one behavior per `test()`, mirroring flags.test.mjs). These tests MUST fail initially because system-rollup.mjs does not yet exist (RED). Do NOT write normalizeSubsystems tests in this task (Task 3 covers them).</action>
  <verify>
    <automated>node --test scripts/system-rollup.test.mjs 2>&1 | grep -q "fail" || echo "EXPECTED RED but tests did not fail"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/system-rollup.test.mjs exists and contains `from './system-rollup.mjs'`
    - test titles contain the strings "STATUS-02" and "STATUS-03"
    - behavior: `node --test scripts/system-rollup.test.mjs` exits non-zero (RED) because the module under test is absent
  </acceptance_criteria>
  <done>Test file committed in RED state; running the test fails because system-rollup.mjs is not yet implemented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement computeRollup + chipLabel to green (STATUS-02/STATUS-03)</name>
  <files>scripts/system-rollup.mjs</files>
  <read_first>
    - scripts/flags.mjs lines 1-18 (module provenance/sync header comment + export style to copy)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 229-251 (verbatim computeRollup/chipLabel bodies)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 57-98 (system-rollup.mjs module header + export contract)
  </read_first>
  <action>Create scripts/system-rollup.mjs. Start with a provenance/sync header comment mirroring scripts/flags.mjs lines 1-9, stating the functions are inlined byte-identical into frontend/index.html (no build step). Export `const TIER_RANK = { green: 0, orange: 1, red: 2 }`. Export `function computeRollup(subsystems)` implementing worst-of-four: iterate subsystems, increment failCount when TIER_RANK[s.tier] > 0, track worst when TIER_RANK[s.tier] > TIER_RANK[worst.tier], return `{ tier: worst.tier, worstName: worst.name, overflow: Math.max(0, failCount - 1) }`. Export `function chipLabel(rollup)`: return "System" when tier==='green' (D-10), else `System: ${rollup.worstName}` plus ` +${rollup.overflow}` when overflow > 0 (D-11). Copy the bodies verbatim from RESEARCH § Pattern 3. No imports, no DOM, no default export.</action>
  <verify>
    <automated>node --test scripts/system-rollup.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/system-rollup.mjs contains `export function computeRollup` and `export function chipLabel` and `export const TIER_RANK`
    - scripts/system-rollup.mjs has the byte-identical-sync header comment referencing frontend/index.html
    - `node --test scripts/system-rollup.test.mjs` exits 0 (all GREEN)
  </acceptance_criteria>
  <done>computeRollup and chipLabel implemented; the Task 1 tests pass (GREEN).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Add normalizeSubsystems with D-05/D-06 mapping (TDD, STATUS-02/STATUS-03)</name>
  <files>scripts/system-rollup.mjs, scripts/system-rollup.test.mjs</files>
  <read_first>
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 352-368 (Metric Preservation Map — verbatim metric/annotation strings)
    - .planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md lines 159-176 (Copywriting Contract — exact row copy)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 100-102, 140-149 (normalizeSubsystems contract + D-05/D-06 assertion examples)
    - frontend/index.html lines 4059-4126 (current setChip branches — the metric strings to preserve verbatim)
  </read_first>
  <behavior>
    - D-05: `normalizeSubsystems({ folio_ontology: { status: 'not_loaded' } })` → FOLIO row tier === 'green', annotation matches /Standby/.
    - D-05: embedding not_loaded → Embedding row tier === 'green', annotation matches /Standby/.
    - D-06: `folio_ontology.update_status.update_available === true` (status ready) → FOLIO tier === 'green', annotation matches /Update/.
    - D-06: `update_status.update_in_progress === true` → FOLIO tier === 'green', annotation matches /Updating/.
    - Error mapping: `spacy.status:'error'` → spaCy row tier === 'red'.
    - Backend-down: when called with backendUp=false, all four rows tier maps appropriately and Backend row reads offline (verify the chosen signature handles this — see action).
    - Order + exclusion: returns exactly 4 rows keyed Backend, FOLIO, Embedding, spaCy in that fixed order; NO llm key present (STATUS-06).
    - Metric preservation: FOLIO ready row metric contains both concepts and "labels indexed"; Embedding ready row contains provider and "vectors indexed"; spaCy ready row contains the version and "EntityRuler ready".
  </behavior>
  <action>Extend scripts/system-rollup.test.mjs with a normalizeSubsystems section (banner comment + D-05/D-06/STATUS tags in titles) covering the <behavior> cases — write these tests FIRST (RED). Then implement `export function normalizeSubsystems(detail, backendUp)` in scripts/system-rollup.mjs returning a fixed-order array `[{ key, name, tier, statusWord, metric, annotation, action? }]` for Backend/FOLIO/Embedding/spaCy ONLY (never read detail.llm). Encode D-05 (`status==='not_loaded'`→tier 'green' + "Standby — loads on first use" annotation) and D-06 (`update_status.update_available`/`update_in_progress`→tier 'green' + "Update available"/"Updating…" annotation) HERE and only here — Standby/Update affect annotation, never tier. Map `status==='error'`→tier 'red'. Set `action: 'manage-folio'` (or equivalent marker) on the FOLIO row only (D-08); no action on other rows. Source every metric/annotation/statusWord string verbatim from the Metric Preservation Map (RESEARCH 352-368) and Copywriting Contract (UI-SPEC 159-176). Keep formatting helpers (e.g. number formatting) inline in the module so node:test stays standalone. Decide and document the backendUp parameter handling so the backend-down case maps all rows to an offline/red presentation consistent with current index.html:4044-4050.</action>
  <verify>
    <automated>node --test scripts/system-rollup.test.mjs scripts/flags.test.mjs scripts/contrast-audit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/system-rollup.mjs contains `export function normalizeSubsystems`
    - source: normalizeSubsystems body contains no reference to `llm` or `detail.llm` (STATUS-06 exclusion) — `grep -v '^//' scripts/system-rollup.mjs | grep -c 'llm'` returns 0
    - behavior: a not_loaded FOLIO/Embedding produces tier 'green' (D-05); update_available/update_in_progress produces tier 'green' (D-06); a spacy error produces tier 'red'
    - behavior: normalizeSubsystems returns exactly 4 rows in order Backend, FOLIO, Embedding, spaCy with no llm key
    - `node --test scripts/system-rollup.test.mjs scripts/flags.test.mjs scripts/contrast-audit.test.mjs` exits 0
  </acceptance_criteria>
  <done>normalizeSubsystems implemented with D-05/D-06 mapping in one place; all named-file tests pass; LLM excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend `/health/detail` → client JS | Backend-supplied subsystem strings (message/provider/version) enter client logic. This plan only computes tier/label from the structured `status` enum and copies metric strings; it does not render to the DOM (Wave 2/3 handle rendering with `textContent`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Info-disclosure | normalizeSubsystems consuming backend `message` strings | accept (in this plan) | Pure logic only; no DOM sink here. DOM-XSS mitigation (textContent, never innerHTML) is enforced where strings are rendered — Wave 3 plan 04. No interpolation into executable contexts in this module. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs this plan/phase (single-file vanilla JS, no build step). Any install task would contradict locked scope and must be rejected. |

No HIGH-severity threats: frontend-only, no auth, no new endpoints, no persisted input, no package installs.
</threat_model>

<verification>
- `node --test scripts/system-rollup.test.mjs scripts/flags.test.mjs scripts/contrast-audit.test.mjs` exits 0 (named-file form per PATTERNS Gotcha 1).
- `scripts/system-rollup.mjs` exports exactly TIER_RANK, normalizeSubsystems, computeRollup, chipLabel (no default export, no DOM, no non-node: imports).
- normalizeSubsystems never reads the LLM subsystem (STATUS-06).
</verification>

<success_criteria>
- STATUS-02: all-green → green "System" (unit-proven).
- STATUS-03: worst-of-four + named subsystem + "+N" overflow (unit-proven).
- D-05/D-06: Standby and Update flags normalize to green tier with distinct annotations (unit-proven).
- Module shape mirrors scripts/flags.mjs exactly so the Wave 2 byte-identical inline copy is mechanical.
</success_criteria>

<output>
Create `.planning/phases/03-consolidated-system-status-chip/03-01-SUMMARY.md` when done. Record the exact `normalizeSubsystems` row-object shape and the backendUp handling decision so Wave 2/3 can paste the byte-identical inline copy and wire it without re-deriving the contract.
</output>
