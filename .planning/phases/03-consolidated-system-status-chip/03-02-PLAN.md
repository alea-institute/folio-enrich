---
phase: 03-consolidated-system-status-chip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/contrast-audit.mjs
  - scripts/contrast-audit.test.mjs
autonomous: true
requirements: [STATUS-05]
must_haves:
  truths:
    - "The audit checks status-icon colors (green/orange/red) as graphical objects at the 3:1 floor on chip/popover surfaces in all three themes"
    - "node --test scripts/ (directory form) no longer fails because of the stale report path"
    - "The audit writes its report to the Phase 03 directory, not the non-existent Phase v1.0 directory"
    - "The test encodes that Light-theme solid green/orange icons fail 3:1 and the --text stroke fallback is mandatory"
  artifacts:
    - path: "scripts/contrast-audit.mjs"
      provides: "Status-icon 3:1 graphical-object audit + corrected report path"
      contains: "surface2"
    - path: "scripts/contrast-audit.test.mjs"
      provides: "Assertions for status-icon contrast and the Light-theme stroke-fallback rule"
      contains: "node:test"
  key_links:
    - from: "scripts/contrast-audit.test.mjs"
      to: "scripts/contrast-audit.mjs"
      via: "ES import of exported helpers"
      pattern: "from './contrast-audit.mjs'"
---

<objective>
Extend the WCAG audit so it actually catches the STATUS-05 risk: status icons rendered as solid `--green`/`--orange` fills FAIL the 3:1 graphical-object floor in the Light theme (research Pitfall 1, computed). Today the audit only checks `--text/--text-dim/--accent` text foregrounds, so it would falsely report "0 FAILs". Also fix the stale hardcoded report path (research Pitfall 2) that currently makes `node --test scripts/` (directory form) fail.

Purpose: Give the Wave 2/3 frontend work a deterministic gate that proves the status-icon stroke-fallback decision is correct in every theme before manual UAT.
Output: extended `scripts/contrast-audit.mjs` (new status-icon loop + corrected report path) + extended `scripts/contrast-audit.test.mjs` (asserting the new pairs and the Light-theme stroke-fallback rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md
@.planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md
@.planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md

<interfaces>
<!-- Exported helpers already in scripts/contrast-audit.mjs (reuse as-is; the test imports these). -->
  export function hexToRgb(hex)
  export function relativeLuminance({ r, g, b })
  export function contrastRatio(fgHex, bgHex)
  export function mixColors(fgHex, bgHex, fgFraction)
  export function parseCssVariables(cssBlock)
  export function resolveVariable(name, themeMap, paletteMap, depth = 0)

<!-- Existing module internals (verified line anchors): -->
  TEXT_TOKENS = ['--text', '--text-dim', '--accent']                 // :99
  BG_TOKENS   = ['--bg', '--surface', '--surface2', '--surface3']    // :100
  // text-on-bg audit loop to mirror                                 // :234-250 (resolveVariable + contrastRatio + classify trio)
  reportPath  = '.planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md'  // :200  STALE — non-existent dir
  // direct-invoke guard                                             // :296
  classify(ratio) -> pass/fail status (text thresholds 4.5/3.0)

<!-- Computed contrast facts (RESEARCH :294, UI-SPEC :72-77 v1.0 tokens) — pin these exact hex literals in tests: -->
  Light --text #1a1d27  on --surface2 #eceef4 → ~13:1  (PASS, stroke fallback)
  Light --green #16a34a on --surface2 #eceef4 → 2.84:1 (FAIL as solid fill)
  Light --orange #d97706 on --surface2 #eceef4 → 2.75:1 (FAIL as solid fill)
  Light --green #16a34a on --surface3 #e2e5ee → 2.62:1 (FAIL)
  Light --orange #d97706 on --surface3 #e2e5ee → 2.53:1 (FAIL)
  // Dark/Mixed green/orange and all-theme red already pass ≥ 3:1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale report path so node --test scripts/ passes</name>
  <files>scripts/contrast-audit.mjs</files>
  <read_first>
    - scripts/contrast-audit.mjs lines 196-298 (reportPath construction at :200, writeFileSync at :289, direct-invoke guard at :296)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 282-304 (path-fix instruction + Gotcha 1: directory-form test currently fails)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 298-302 (Pitfall 2)
  </read_first>
  <action>In scripts/contrast-audit.mjs, change the hardcoded reportPath at line 200 from `.planning/phases/03-accessibility-component-polish/03-AUDIT-REPORT.md` to `.planning/phases/03-consolidated-system-status-chip/03-AUDIT-REPORT.md` (the existing Phase 03 directory). This is the directory that makes `node --test scripts/` (directory form) currently fail because the audit module's report write targets a non-existent dir. Make no other behavioral change in this task.</action>
  <verify>
    <automated>node --test scripts/ 2>&1 | tail -5; node --test scripts/system-rollup.test.mjs scripts/flags.test.mjs scripts/contrast-audit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - source: `grep -c "03-accessibility-component-polish" scripts/contrast-audit.mjs` returns 0
    - source: scripts/contrast-audit.mjs contains the string `03-consolidated-system-status-chip/03-AUDIT-REPORT.md`
    - behavior: `node scripts/contrast-audit.mjs` runs and writes the report into the Phase 03 directory without a path error
    - behavior: `node --test scripts/` (directory form) no longer fails on the report-path import
  </acceptance_criteria>
  <done>Report path points at the Phase 03 directory; the audit runs and writes successfully; directory-form test run no longer fails on this.</done>
</task>

<task type="auto">
  <name>Task 2: Add the status-icon 3:1 graphical-object audit loop</name>
  <files>scripts/contrast-audit.mjs</files>
  <read_first>
    - scripts/contrast-audit.mjs lines 99-115 (TEXT_TOKENS/BG_TOKENS + classify), lines 234-250 (the text-on-bg loop to mirror), lines 251-291 (results aggregation + markdown report)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 289-302 (parallel STATUS_ICON_TOKENS loop instruction)
    - .planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md lines 90-93 (3:1 graphical-object floor for status icons)
  </read_first>
  <action>In scripts/contrast-audit.mjs add a `STATUS_ICON_TOKENS = ['--green', '--orange', '--red']` foreground set and a parallel audit loop, mirroring the existing text-on-bg loop at :234-250 (same resolveVariable + contrastRatio trio), testing each status token against `['--surface2', '--surface3']` (chip + popover surfaces) at a 3:1 PASS floor across all three themes. Mark these results as graphical-object (3:1) rows distinct from the 4.5/3.0 text rows so the report and the audit's overall pass/fail reflect the 3:1 threshold for icons. Include the new rows in the markdown report output. Run against the current tokens: this loop is EXPECTED to surface the Light-theme green/orange FAILs (2.5–2.8:1) as documented — that is correct behavior; the implementation in Wave 3 resolves them via the `--text` stroke fallback (the icons are stroked at --text, which passes ≥13:1, with status color as a secondary fill). Do NOT weaken the threshold to hide the FAIL.</action>
  <verify>
    <automated>node scripts/contrast-audit.mjs 2>&1 | grep -iE "green|orange|red|surface2|surface3" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - source: scripts/contrast-audit.mjs contains `--green`, `--orange`, `--red` as audited foreground tokens and references `--surface2` and `--surface3` as their backgrounds
    - source: the new loop uses a 3:1 floor (the string `3` / `3.0` threshold present in the icon-check path, distinct from text 4.5)
    - behavior: `node scripts/contrast-audit.mjs` output includes status-icon rows for green/orange/red on surface2/surface3 across Dark/Light/Mixed
    - behavior: the Light-theme solid green and orange on surface2/surface3 are reported as failing the 3:1 floor (proving the audit now catches Pitfall 1)
  </acceptance_criteria>
  <done>Audit now checks status-icon colors as 3:1 graphical objects on chip/popover surfaces in all themes and surfaces the documented Light-theme green/orange FAILs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert status-icon contrast facts in contrast-audit.test.mjs</name>
  <files>scripts/contrast-audit.test.mjs</files>
  <read_first>
    - scripts/contrast-audit.test.mjs lines 1-37 (existing import + assertion style to extend)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 308-334 (status-icon assertion examples + exact hex literals)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md line 294 (computed ratios)
  </read_first>
  <behavior>
    - STATUS-05: Light `--text` #1a1d27 on `--surface2` #eceef4 contrastRatio ≥ 3.0 (the stroke fallback passes).
    - STATUS-05: Light solid `--green` #16a34a on `--surface2` #eceef4 contrastRatio < 3.0 (documents why stroke fallback is mandatory).
    - STATUS-05: Light solid `--orange` #d97706 on `--surface2` #eceef4 contrastRatio < 3.0.
    - STATUS-05: Light `--text` #1a1d27 on `--surface3` #e2e5ee contrastRatio ≥ 3.0.
  </behavior>
  <action>Extend scripts/contrast-audit.test.mjs (do NOT rewrite existing tests). Import `contrastRatio` (already imported) and add a STATUS-05 section (banner comment + STATUS-05 in titles) asserting the <behavior> facts using the exact hex literals from PATTERNS lines 308-334 / RESEARCH line 294 (Light --text #1a1d27, --green #16a34a, --orange #d97706, --surface2 #eceef4, --surface3 #e2e5ee). Each test pins a documented ratio: the --text stroke clears 3:1; solid green/orange fail 3:1. These encode that the Light-theme stroke fallback is mandatory, not optional.</action>
  <verify>
    <automated>node --test scripts/contrast-audit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - source: scripts/contrast-audit.test.mjs test titles contain "STATUS-05"
    - source: tests reference the exact hex literals #1a1d27, #16a34a, #d97706, #eceef4, #e2e5ee
    - behavior: assertions prove --text stroke ≥ 3.0 AND solid green/orange < 3.0 on Light surfaces
    - `node --test scripts/contrast-audit.test.mjs` exits 0
  </acceptance_criteria>
  <done>Tests encode the mandatory Light-theme --text stroke fallback and the solid green/orange 3:1 failure; the test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem ← audit script | The audit reads frontend/index.html and writes a markdown report into the repo. No network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | reportPath file write | accept | Path is a repo-relative constant pointing at a tracked planning directory; no user input flows into it. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs (Node built-in test runner, zero-dep audit). Any install task contradicts locked scope and must be rejected. |

No HIGH-severity threats: a zero-dependency local audit/test script with no network and no untrusted input.
</threat_model>

<verification>
- `node --test scripts/` (directory form) no longer fails on the report-path import (PATTERNS Gotcha 1).
- `node scripts/contrast-audit.mjs` runs, writes to the Phase 03 directory, and reports status-icon green/orange/red on surface2/surface3 in all themes.
- `node --test scripts/contrast-audit.test.mjs scripts/flags.test.mjs` exits 0.
</verification>

<success_criteria>
- STATUS-05 gate exists: the audit deterministically catches Light-theme solid-icon 3:1 failures (Pitfall 1) instead of falsely reporting 0 FAILs (Pitfall 2).
- The stale report path is fixed so the test suite's directory form is unblocked.
- Tests pin the computed ratios so the stroke-fallback rule cannot regress.
</success_criteria>

<output>
Create `.planning/phases/03-consolidated-system-status-chip/03-02-SUMMARY.md` when done. Note exactly which status-icon pairs FAIL as solid fills (so Wave 3 knows it MUST stroke those glyphs at --text) and confirm `node --test scripts/` directory form now passes.
</output>
