---
phase: 03-consolidated-system-status-chip
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - frontend/index.html
autonomous: false
requirements: [STATUS-01, STATUS-03, STATUS-04, STATUS-05, STATUS-06, STATUS-07]
must_haves:
  truths:
    - "checkHealth() drives the one System chip + four popover rows from /health/detail; the LLM/Ollama branch is byte-for-byte unchanged"
    - "The collapsed chip shows the rollup glyph + 'System' (healthy) or 'System: {Subsystem} +N' (degraded), updating live every poll"
    - "Click and Enter/Space open the popover; Escape, outside-click, and re-activation close it; focus moves into the popover on open and restores to the chip on close"
    - "Popover rows update in place on each poll without stealing focus or rebuilding the DOM"
    - "All metrics currently shown via setChip tooltips are preserved in the popover rows"
    - "Backend-down fallback and the FOLIO completed-update toast still work"
  artifacts:
    - path: "frontend/index.html"
      provides: "Refactored checkHealth + render functions + disclosure open/close + Escape/keydown wiring"
      contains: "renderPopoverRows"
  key_links:
    - from: "frontend/index.html checkHealth()"
      to: "inline normalizeSubsystems/computeRollup/chipLabel"
      via: "normalize → rollup → render call chain"
      pattern: "computeRollup\\(normalizeSubsystems"
    - from: "frontend/index.html #chipSystem click/keydown"
      to: "#systemStatusPopover open/close"
      via: "disclosure toggle with focus management"
      pattern: "aria-expanded"
---

<objective>
Wire the consolidated System chip to live health data and accessible disclosure behavior. Refactor `checkHealth()` so the four subsystem branches feed `normalizeSubsystems → computeRollup → renderSystemChip/renderPopoverRows`, while leaving the `d.llm`/Ollama branch byte-for-byte intact (STATUS-06). Implement the disclosure open/close with focus management (D-04), live in-place row updates (D-03), and the FOLIO Manage action. Render all backend strings via `textContent` (DOM-XSS mitigation). Preserve the backend-down fallback and the FOLIO completed-update toast.

Purpose: Make the chip functional, accessible, and metric-complete — the behavioral payload of STATUS-01/03/04/05, with STATUS-06/07 protected.
Output: `frontend/index.html` with refactored `checkHealth()`, new render + disclosure functions, extended Escape/keydown wiring, and `setChip()` retained for LLM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md
@.planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md
@.planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md
@.planning/phases/03-consolidated-system-status-chip/03-01-SUMMARY.md
@.planning/phases/03-consolidated-system-status-chip/03-03-SUMMARY.md

<interfaces>
<!-- Inline rollup functions available from plan 03 (byte-identical to scripts/system-rollup.mjs): -->
  normalizeSubsystems(detail, backendUp) -> [{ key, name, tier, statusWord, metric, annotation, action? }]  // Backend,FOLIO,Embedding,spaCy; LLM excluded
  computeRollup(subsystems) -> { tier, worstName, overflow }
  chipLabel(rollup) -> "System" | "System: {worstName}" | "System: {worstName} +{overflow}"

<!-- checkHealth() to refactor (frontend/index.html:4038-4130). PRESERVE verbatim: -->
  :4044-4050  backend-down fallback (chip red + all rows offline)
  :4074-4081  FOLIO completed-update toast using _lastFolioUpdateAt
  :4100-4118  d.llm branch + `await updateOllamaChip()` + setChip('chipLLM', ...)  ← DO NOT TOUCH (STATUS-06)
  :4011       setInterval(checkHealth, 10000)  ← cadence unchanged

<!-- setChip() (:4021-4036) — KEEP; LLM + Ollama (:5016-5026) still call it. Note its textContent discipline (:4028). -->

<!-- Disclosure mechanics (RESEARCH lines 200-225): openSystemPopover/closeSystemPopover with focus in/restore,
     Escape + outside-click (deferred listener via setTimeout(...,0) — Pitfall 5), no focus trap (non-modal). -->

<!-- Reuse existing plumbing (Don't-Hand-Roll, RESEARCH 261-273): -->
  Escape handler   :10388-10401  (extend with a #systemStatusPopover-open branch)
  chip Enter/Space :10404-10411  (querySelectorAll('.status-chip.clickable') → click) — #chipSystem has .clickable so it inherits

<!-- Copy/metric sources: Metric Preservation Map (RESEARCH 352-368), Copywriting Contract (UI-SPEC 159-176). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor checkHealth() to drive the chip + rows; render functions</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html lines 4011, 4021-4036 (setInterval + setChip to KEEP), lines 4038-4130 (checkHealth — full body incl. backend-down fallback, FOLIO toast, d.llm branch)
    - frontend/index.html lines 10297+ (the inline normalizeSubsystems/computeRollup/chipLabel from plan 03)
    - .planning/phases/03-consolidated-system-status-chip/03-03-SUMMARY.md (popover row DOM structure, status-color state-class names, icon-slot selector — render targets)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 152-162, 352-368 (component responsibilities + Metric Preservation Map)
    - .planning/phases/03-consolidated-system-status-chip/03-PATTERNS.md lines 224-244, 304-307 (checkHealth refactor cluster (c) + preserve toast/fallback)
  </read_first>
  <behavior>
    - Healthy: mock /health/detail all-ready → #chipSystem shows the check glyph + label "System"; all four rows show their metrics.
    - Standby (D-05): folio/embedding not_loaded → chip stays green/"System"; FOLIO + Embedding rows read "Standby — loads on first use".
    - Degraded (STATUS-03): mock spacy.status:"error" → chip shows cross glyph (.is-red) + label "System: spaCy"; a 2nd failure → "System: spaCy +1"; popover names all failures.
    - Metric preservation (STATUS-04): FOLIO ready row contains concepts + "labels indexed"; Embedding ready row contains provider + "vectors indexed"; spaCy ready row contains version + "EntityRuler ready".
    - Backend-down: /health not ok → chip red, rows offline (existing :4044-4050 behavior preserved).
    - LLM untouched (STATUS-06): the d.llm branch + updateOllamaChip() still set #chipLLM exactly as before.
    - FOLIO toast: a changed last_update_at still fires _showInfoToast (existing :4074-4081 behavior preserved).
  </behavior>
  <action>In frontend/index.html, refactor checkHealth() (:4038-4130). Replace the four `setChip('chipBackend'/'chipFolio'/'chipEmbedding'/'chipSpacy', ...)` branches with: `const subsystems = normalizeSubsystems(d, backendUp); renderSystemChip(computeRollup(subsystems)); renderPopoverRows(subsystems);`. Capture backendUp from the /health probe (the try/catch at :4040-4051). In the backend-down catch, call the same normalize/render path so the chip goes red and rows read offline — preserving the :4044-4050 intent (do NOT delete that behavior, route it through the new functions). Leave the `d.llm` branch (:4100-4118), `await updateOllamaChip()`, and the FOLIO completed-update toast block (:4074-4081 using _lastFolioUpdateAt) byte-for-byte intact (STATUS-06; toast preservation). Keep setChip() (:4021-4036) — LLM/Ollama still call it. Implement `renderSystemChip(rollup)`: set the chip's status-icon slot to the rollup glyph + status-color state-class (from the 03-03-SUMMARY selectors), set #chipSystemLabel via chipLabel(rollup), and set aria attributes as needed. Implement `renderPopoverRows(subsystems)`: update each existing row's icon/name/status/metric/annotation IN PLACE via `textContent` (never innerHTML of message strings — DOM-XSS, mirror setChip :4028); be idempotent so live polls don't tear down DOM or steal focus (Pitfall 3); the FOLIO row's existing "Manage FOLIO" action is left in place (it already calls openFolioModal()).</action>
  <verify>
    <automated>node --test scripts/system-rollup.test.mjs scripts/contrast-audit.test.mjs; grep -c "computeRollup(normalizeSubsystems\|renderSystemChip\|renderPopoverRows" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - source: frontend/index.html checkHealth contains a call chain using `normalizeSubsystems(` and `computeRollup(` and `renderSystemChip(` and `renderPopoverRows(`
    - source: frontend/index.html still contains `function setChip` and the unchanged `d.llm` branch with `updateOllamaChip()` and `_lastFolioUpdateAt` toast block
    - source: renderPopoverRows uses `textContent` (or safe DOM APIs) and contains no `innerHTML =` with interpolated message strings (DOM-XSS) — `grep -n "innerHTML" frontend/index.html` shows no new message-string interpolation in the render path
    - behavior (manual UAT, per VALIDATION.md): mocking spacy.status:"error" makes the chip render "System: spaCy" with the cross-circle (.is-red) icon; a 2nd failure shows "System: spaCy +1"
    - behavior: all of today's metrics (concepts, labels indexed, vectors indexed, provider, spaCy version) appear in the popover rows (diff vs Metric Preservation Map)
    - `node --test scripts/system-rollup.test.mjs scripts/contrast-audit.test.mjs` exits 0
  </acceptance_criteria>
  <done>checkHealth drives one chip + four live rows from the rollup functions; metrics preserved; backend-down fallback and FOLIO toast intact; LLM/Ollama path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Disclosure open/close with focus management + Escape/outside-click (D-04)</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html lines 10388-10411 (existing Escape handler + .status-chip.clickable Enter/Space — extend, don't duplicate)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 189-225 (Pattern 2 non-modal popover: openSystemPopover/closeSystemPopover, deferred outside-click, no focus trap), lines 304-318 (Pitfall 5 deferred listener)
    - .planning/phases/03-consolidated-system-status-chip/03-UI-SPEC.md lines 143-153 (Interaction & Accessibility Contract)
  </read_first>
  <action>In frontend/index.html add `openSystemPopover()` and `closeSystemPopover()` (adapt RESEARCH lines 200-225). openSystemPopover: unhide #systemStatusPopover, set #chipSystem `aria-expanded="true"`, move focus into the popover (focus the `tabindex="-1"` region or the FOLIO "Manage FOLIO" action), register the Escape + outside-click listeners, and DEFER the document outside-click listener with `setTimeout(..., 0)` so the opening click does not immediately close it (Pitfall 5). closeSystemPopover: hide the popover, set `aria-expanded="false"`, remove the listeners, and restore focus to #chipSystem. Wire #chipSystem activation to a toggle (open when collapsed, close when expanded). #chipSystem already has the `.status-chip.clickable` class so the existing Enter/Space keydown handler at :10404-10411 activates it for free — verify it does; do NOT add a duplicate keydown listener. Extend the existing Escape handler at :10388-10401 with a `#systemStatusPopover`-open branch (close it on Escape) ahead of the modal checks, keeping Escape logic in one place. Outside-click closes when the click target is outside both the chip and the popover. Do NOT implement a focus trap — the popover is non-modal (RESEARCH anti-pattern). Do NOT touch the FOLIO/graph modal Escape branches.</action>
  <verify>
    <automated>grep -c "openSystemPopover\|closeSystemPopover\|aria-expanded" frontend/index.html; grep -c "setTimeout" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - source: frontend/index.html contains `function openSystemPopover` and `function closeSystemPopover`
    - source: openSystemPopover sets `aria-expanded` to "true", focuses the popover/Manage action, and registers the outside-click listener via `setTimeout(`; closeSystemPopover sets `aria-expanded` "false" and calls `.focus()` on the chip
    - source: the Escape handler at ~:10388 has a #systemStatusPopover branch; the FOLIO/graph modal branches are unchanged
    - source: no new keydown listener for Enter/Space (reuses the existing `.status-chip.clickable` handler); no focus-trap loop added
    - behavior (manual UAT): click and Enter/Space both open; Escape, outside-click, and re-activation close; focus moves into the popover on open and restores to the chip on close
  </acceptance_criteria>
  <done>Accessible non-modal disclosure works via click + Enter/Space to open and Escape/outside-click/re-activate to close, with focus moved in on open and restored on close; no focus trap; existing Escape/keydown plumbing reused.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT across Dark/Light/Mixed (STATUS-01/03/04/05/06/07)</name>
  <read_first>
    - .planning/phases/03-consolidated-system-status-chip/03-VALIDATION.md lines 66-77 (Manual-Only Verifications table)
    - .planning/phases/03-consolidated-system-status-chip/03-RESEARCH.md lines 440-448 (Manual UAT checklist)
  </read_first>
  <what-built>
    A consolidated System disclosure chip + four-row anchored popover replacing the four subsystem chips, driven by /health/detail, with accessible open/close, live in-place updates, and the FOLIO Manage action — LLM chip untouched. Automated gates already pass: `node --test scripts/` and `node scripts/contrast-audit.mjs` (zero FAILs incl. status-icon 3:1 in all themes).
  </what-built>
  <how-to-verify>
    Start the backend: `cd backend && .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8731 --reload`. Open the frontend at http://localhost:8732. Use Chrome DevTools MCP (navigate_page → wait_for → take_screenshot) — NOT headless. Check across all three themes (toggle data-theme Dark/Light/Mixed):
    1. STATUS-02 quiet-green: fresh load → chip shows check icon + "System" (green); open popover → 4 rows; FOLIO/Embedding read "Standby — loads on first use" but chip stays green.
    2. STATUS-04 expand/metrics: click AND Enter/Space both open; all of today's metrics present (concepts, labels indexed, vectors indexed, spaCy version, provider); FOLIO row "Manage FOLIO" opens the existing modal unchanged (D-08).
    3. STATUS-03 degraded: mock /health/detail (override fetch in console) with spacy.status:"error" → chip red + "System: spaCy"; add a 2nd failure → "System: spaCy +1"; popover names all failures.
    4. D-03 live update: with popover open, let a poll change FOLIO Standby→ready → row + rollup update in place; focus not stolen; no SR spam.
    5. STATUS-05 a11y: keyboard-only open/navigate/close (Enter/Space/Escape/outside-click/Tab); focus moves in on open, restores to chip on close; status conveyed by icon shape + word (verify Light-theme green/orange icons use the --text stroke and read in grayscale).
    6. STATUS-06 LLM: LLM chip still separate, still configures via its click.
    7. STATUS-07 overlap: after a document loads (layer chips visible), #statusBar does not overlap #layerToggleBar at normal desktop width. If residual overlap remains, apply flex-wrap/min-width:0 on .status-bar (UI-SPEC :139) and re-verify.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (which theme, which checklist item).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend `/health/detail` strings → popover DOM | Backend-supplied message/provider/version strings are rendered into popover rows. This is the one live DOM sink in the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering/Info-disclosure (DOM-XSS) | renderPopoverRows injecting f.message/e.message/s.message/provider/version | mitigate | Render via `textContent`/safe DOM APIs only; NEVER `innerHTML` with interpolated backend strings. Mirrors the existing setChip discipline at index.html:4028. Verified by grep gate in Task 1 acceptance. |
| T-03-05 | Denial of Service (focus/SR) | 10s poll re-rendering open popover | accept (mitigated by design) | Update text in place, never rebuild rows; rows `aria-live="off"`; optional single polite region only on tier change — avoids focus theft and SR spam (Pitfall 3). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs (single-file vanilla JS, no build step). Any install task contradicts locked scope and must be rejected. |

No HIGH-severity threats: frontend-only, no auth, no new endpoints. The one real control (DOM-XSS via backend strings) is mitigated with textContent.
</threat_model>

<verification>
- `node --test scripts/system-rollup.test.mjs scripts/contrast-audit.test.mjs scripts/flags.test.mjs` exits 0; `node scripts/contrast-audit.mjs` exits 0 with zero FAILs.
- `cd backend && .venv/bin/python -m pytest tests/ -v` stays green (backend untouched — regression guard).
- Manual UAT checklist (Task 3) complete across Dark/Light/Mixed: STATUS-01/02/03/04/05/06/07 all confirmed.
- grep confirms no new `innerHTML` interpolation of backend message strings in the render path (DOM-XSS mitigation).
</verification>

<success_criteria>
- STATUS-01/04: one chip + four-row popover with all metrics preserved, driven by /health/detail.
- STATUS-03: worst-status rollup names the failing subsystem with +N overflow, live.
- STATUS-05: icon + text + WCAG AA; keyboard + SR accessible; non-modal disclosure with focus in/restore.
- STATUS-06: LLM chip + onLLMChipClick() + updateOllamaChip() + setChip unchanged.
- STATUS-07: no header overlap after document load (flex-wrap/min-width:0 fallback applied only if residual).
</success_criteria>

<output>
Create `.planning/phases/03-consolidated-system-status-chip/03-04-SUMMARY.md` when done. Record the UAT outcomes per theme, any STATUS-07 flex-wrap fallback applied, and confirm the LLM path and backend tests are unchanged/green.
</output>
