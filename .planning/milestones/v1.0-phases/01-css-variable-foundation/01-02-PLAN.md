---
phase: 01-css-variable-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/index.html
autonomous: true
requirements:
  - CSSF-01
  - CSSF-07

must_haves:
  truths:
    - "Zero hardcoded hex or rgba color values remain in CSS rules outside the :root primitive definitions"
    - "All 104 data-branch CSS rules consolidated into 52 rules (26 normal + 26 hover) using color-mix() with var() branch colors"
    - "The old .panel-right and .panel-detail variable override blocks are removed (replaced by [data-theme='mixed'] selectors from Plan 01)"
    - "Every CSS rule that previously referenced a hardcoded color now references a var() token"
    - "Application appearance in mixed mode is identical to before the conversion"
  artifacts:
    - path: "frontend/index.html"
      provides: "All CSS rules converted to use var() references"
      contains: "color-mix(in srgb, var(--branch-"
  key_links:
    - from: "CSS rules using var(--bg), var(--surface), etc."
      to: "[data-theme] semantic token definitions"
      via: "CSS custom property cascade"
      pattern: "background:\\s*var\\(--"
    - from: ".annotation-span[data-branch] rules"
      to: "--branch-* variables in theme blocks"
      via: "color-mix() with var() branch color"
      pattern: "color-mix\\(in srgb, var\\(--branch-"
---

<objective>
Replace ALL hardcoded hex, rgb, rgba, and named color values in the CSS rules with var() references to the semantic tokens defined in Plan 01. Consolidate 104 data-branch rules into 52 rules (26 normal + 26 hover) using color-mix(). Remove the old .panel-right/.panel-detail variable override blocks.

Purpose: This is the core migration -- after this plan, every color in the CSS is controlled by the variable system. Theme switching becomes possible because all rules reference variables that theme selectors redefine.

Output: frontend/index.html with zero hardcoded color values in CSS rules (only palette primitives in :root retain raw hex values). Branch rules consolidated. Old panel overrides removed.

Note on D-07 ("one big commit"): This plan is one of three sequential plans that together implement D-07's intent. Each plan produces a non-breaking intermediate state (Plan 01: define variables, Plan 02: replace colors, Plan 03: audit + verify). No plan leaves the app in a broken state, satisfying D-07's goal of avoiding partial/mixed states.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-variable-foundation/01-CONTEXT.md
@.planning/phases/01-css-variable-foundation/01-RESEARCH.md
@.planning/phases/01-css-variable-foundation/01-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these semantic tokens are available for use in CSS rules: -->

Core tokens: --bg, --surface, --surface2, --surface3, --border, --text, --text-dim, --accent, --accent-dim
Status tokens: --green, --orange, --red, --cyan, --purple
Highlight tokens: --highlight, --highlight-confirmed, --highlight-preliminary, --highlight-rejected
Modal tokens: --modal-bg, --modal-text, --modal-text-dim, --modal-border, --modal-input-bg, --modal-input-border
Tooltip tokens: --tooltip-bg, --tooltip-border, --tooltip-text
Graph tokens: --graph-header-bg, --graph-header-text, --graph-header-border
Scrollbar tokens: --scrollbar-thumb, --scrollbar-thumb-hover
Confidence tokens: --conf-high, --conf-high-bg, --conf-mid, --conf-mid-bg, --conf-low, --conf-low-bg
Feedback tokens: --feedback-positive, --feedback-positive-bg, --feedback-negative, --feedback-negative-bg
Individual tokens: --individual-citation, --individual-entity
Property tokens: --property-underline
Layer chip tokens: --layer-concepts, --layer-individuals, --layer-properties, --layer-pos
POS tokens: --pos-noun, --pos-propn, --pos-verb, --pos-adj, --pos-adv
Misc tokens: --undo-toast-bg, --badge-dot
Branch tokens: --branch-actor-player through --branch-system-identifiers, --branch-default

<!-- color-mix() pattern for branch backgrounds: -->
<!-- background: color-mix(in srgb, var(--branch-X) 12%, transparent); -->
<!-- hover background: color-mix(in srgb, var(--branch-X) 22%, transparent); -->
<!--
  CSSF-07 note: The same 12%/22% opacity percentages are used across all themes.
  CSSF-07 requires "branch color background opacity adapts per theme (lower opacity on
  light backgrounds)". This is satisfied by D-05's approach: light themes use entirely
  different, darker branch color values (e.g., dark: #5ec4d4, light: #1e6fa0). When
  color-mix() applies 12% of a darker color onto a white background, the result is
  inherently more subtle than 12% of a bright color on a dark background -- achieving
  the WCAG contrast intent of CSSF-07 without needing different percentage values.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all hardcoded CSS colors to var() references and consolidate branch rules</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (FULL file -- read in sections: lines 1-500, 500-1000, 1000-1500, 1500-2000, 2000-2463 for complete CSS audit)
    - .planning/phases/01-css-variable-foundation/01-01-SUMMARY.md (to see what tokens were defined and any deviations from plan)
    - .planning/phases/01-css-variable-foundation/01-RESEARCH.md (Architecture Patterns: Branch Color Variable Pattern, Migration Strategy, Anti-Patterns to Avoid, Common Pitfalls 2 and 3)
  </read_first>
  <action>
**This is the largest single task in the phase. Work systematically through the CSS block (lines ~8-2463 after Plan 01's additions) replacing every hardcoded color with the appropriate var() reference.**

**Step 1: Take a "before" screenshot** using Chrome DevTools MCP of the app at http://localhost:8731 (start the server first if needed with `cd backend && .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8731 --reload &`). Save to `$HOME/before-theme-migration.png`. This is for D-08 comparison.

**Step 2: Remove the old .panel-right variable override block (lines ~335-354 before Plan 01 changes).**

The old block looks like:
```css
.panel-right {
  --bg: #ffffff;
  --surface: #f5f6f8;
  /* ... 15 variable overrides ... */
  background: var(--bg);
  color: var(--text);
}
```
KEEP the structural properties (`background: var(--bg); color: var(--text);`) but REMOVE the variable redefinitions (they are now handled by the `[data-theme="mixed"] .panel-right` selector from Plan 01). The result should be:
```css
.panel-right {
  background: var(--bg);
  color: var(--text);
}
```

**Do the same for .panel-detail (lines ~364-383 before Plan 01 changes).** Remove the variable redefinitions, keep the structural properties:
```css
.panel-detail {
  background: var(--bg);
  color: var(--text);
  border-left: 1px solid var(--border);
}
```

**Step 3: Replace all hardcoded colors in CSS rules.** Work through the entire CSS block systematically, rule by rule. Here is the mapping strategy:

**Core background/text colors:**
| Hardcoded Value | Replace With |
|----------------|-------------|
| `#0f1117` | `var(--bg)` |
| `#1a1d27` (as background) | `var(--surface)` |
| `#242836` | `var(--surface2)` |
| `#2e3348` | `var(--surface3)` or `var(--border)` depending on context |
| `#e4e6f0` | `var(--text)` |
| `#8b8fa3` | `var(--text-dim)` |
| `#6c8cff` | `var(--accent)` |
| `#4a5fa0` | `var(--accent-dim)` |
| `#4caf7c` | `var(--green)` |
| `#e8a54c` | `var(--orange)` |
| `#e05555` | `var(--red)` |
| `#5ec4d4` | `var(--cyan)` |
| `#b07ee8` | `var(--purple)` |

**Light-panel specific (inside .panel-right/.panel-detail/.detail-section rules):**
| Hardcoded Value | Replace With |
|----------------|-------------|
| `#ffffff` | `var(--bg)` (when in panel/modal context) or `var(--modal-bg)` (when in modal context) |
| `#f5f6f8`, `#f5f6fa` | `var(--surface)` |
| `#ecedf1`, `#eceef4` | `var(--surface2)` |
| `#e2e4ea`, `#e2e5ee` | `var(--surface3)` |
| `#d0d2da`, `#d0d4e0` | `var(--border)` |
| `#1a1a2e`, `#1a1d27` | `var(--text)` |
| `#6b6b80`, `#5c6070` | `var(--text-dim)` |
| `#3b64e0`, `#2d5ee0` | `var(--accent)` |
| `#5a7ae8` | `var(--accent-dim)` |

**Modal colors (around lines 2003-2070):**
| Hardcoded Value | Replace With |
|----------------|-------------|
| `#ffffff` (modal background) | `var(--modal-bg)` |
| `#1a1a2e` (modal text) | `var(--modal-text)` |
| `#d0d0da` (modal border) | `var(--modal-border)` |
| `#4a4a60` (modal label) | `var(--modal-text-dim)` |
| `#f0f0f5` (modal input bg) | `var(--modal-input-bg)` |
| `#c0c0d0` (modal input border) | `var(--modal-input-border)` |

**Graph modal colors (around lines 655-715):**
| Hardcoded Value | Replace With |
|----------------|-------------|
| `#1a1d27` (graph header bg) | `var(--graph-header-bg)` |
| Graph header text | `var(--graph-header-text)` |
| Graph header border | `var(--graph-header-border)` |

**Scrollbar colors:**
| Hardcoded Value | Replace With |
|----------------|-------------|
| `#c0c0cc` (thumb) | `var(--scrollbar-thumb)` |
| `#9090a0` (thumb hover) | `var(--scrollbar-thumb-hover)` |
| `#555570` (dark thumb) | `var(--scrollbar-thumb)` |

**rgba values:** Convert rgba values to use semantic tokens where possible:
- `rgba(108,140,255,0.15)` -> `var(--highlight)`
- `rgba(108,140,255,0.22)` -> `var(--highlight-confirmed)`
- `rgba(108,140,255,0.08)` -> `var(--highlight-preliminary)`
- `rgba(224,85,85,0.08)` -> `var(--highlight-rejected)`
- For rgba values that are semi-transparent versions of a known color, use `color-mix()`:
  e.g., `rgba(76,175,124,0.12)` -> `color-mix(in srgb, var(--green) 12%, transparent)`

**`transparent` is intentional -- do NOT convert.** The value `transparent` is a valid CSS keyword, not a hardcoded color.

**Step 4: Consolidate the 104 data-branch rules (lines ~1001-1060) into 52 rules (26 normal + 26 hover) using color-mix().**

Replace the 26 normal-state rules AND 26 hover-state rules with this pattern:

```css
.annotation-span[data-branch="Actor / Player"]        { border-bottom-color: var(--branch-actor-player); background: color-mix(in srgb, var(--branch-actor-player) 12%, transparent); }
.annotation-span[data-branch="Area of Law"]            { border-bottom-color: var(--branch-area-of-law); background: color-mix(in srgb, var(--branch-area-of-law) 12%, transparent); }
/* ... all 26 branches ... */
```

And 26 hover rules:
```css
.annotation-span[data-branch="Actor / Player"]:hover        { background: color-mix(in srgb, var(--branch-actor-player) 22%, transparent); }
.annotation-span[data-branch="Area of Law"]:hover            { background: color-mix(in srgb, var(--branch-area-of-law) 22%, transparent); }
/* ... all 26 branches ... */
```

The opacity percentages should match the original values: most branches use 10-12% normal and 20-22% hover. Standardize to 12% normal and 22% hover for consistency. Note: These same percentages work across all themes because D-05's approach uses entirely different (darker) branch color values for light themes, so 12% of a dark color on white is inherently more subtle than 12% of a bright color on dark -- satisfying CSSF-07's WCAG intent.

Also consolidate the `.clean-view .annotation-span[data-branch="..."]` rules (lines ~1410-1440). These all set `background: transparent !important;` -- since they are all identical, replace the 26 individual rules with a single rule:
```css
.clean-view .annotation-span[data-branch] { background: transparent !important; }
```

For the clean-view hover variants (lines ~1445-1470), replace with:
```css
.clean-view .annotation-span[data-branch]:hover { background: color-mix(in srgb, var(--branch-actor-player) 8%, transparent) !important; }
```
Wait -- each branch has its own color. Keep the 26 clean-view hover rules but convert them to use `var()` + `color-mix()`:
```css
.clean-view .annotation-span[data-branch="Actor / Player"]:hover { background: color-mix(in srgb, var(--branch-actor-player) 8%, transparent) !important; }
/* ... */
```

**Step 5: Handle inline style attributes in static HTML elements.**
Search for `style="` in the HTML body (lines 2463+) that contain hardcoded colors. Convert:
```html
style="color:#6b6b80" -> style="color:var(--text-dim)"
style="color:#1a1a2e" -> style="color:var(--text)"
```
Note: `style="color:var(--text-dim)"` is valid CSS -- custom properties resolve in inline styles.

**Step 6: Final verification grep.**
Run: `grep -nP '#[0-9a-fA-F]{3,8}' frontend/index.html | grep -v ':root' | grep -v '/\*' | grep -v 'BRANCH_COLORS' | head -50`

Any remaining hex values outside of `:root` (primitives), comments, and JavaScript should be investigated and converted. The ONLY acceptable remaining hex values are:
1. Inside the `:root { }` palette primitive block
2. Inside JavaScript code (BRANCH_COLORS object and template literals -- Phase 2 scope)
3. Inside HTML comments
4. The color `transparent` (not a hex value)

**What NOT to do:**
- Do NOT modify JavaScript code (lines 2464+) -- that is Phase 2 scope
- Do NOT use `!important` for theme overrides (specificity strategy handles this)
- Do NOT collapse near-duplicate colors that serve different semantic purposes (see Pitfall 6)
- Do NOT remove `transparent` values -- they are intentional
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-enrich" && echo "=== Hex colors in CSS outside variable definitions (should be 0) ===" && sed -n '/<style>/,/<\/style>/p' frontend/index.html | grep -vE '^\s*--[a-z]' | grep -cP '#[0-9a-fA-F]{3,8}' && echo "=== color-mix usage count (expect >= 52) ===" && grep -c 'color-mix' frontend/index.html && echo "=== Old panel-right var overrides (should be 0) ===" && grep -A2 '\.panel-right {' frontend/index.html | grep -c '\-\-bg:' && echo "=== data-branch rule count ===" && grep -c 'data-branch=' frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Hex colors in CSS rules outside variable-definition lines is 0: `sed -n '/<style>/,/<\/style>/p' frontend/index.html | grep -vE '^\s*--[a-z]' | grep -cP '#[0-9a-fA-F]{3,8}'` returns 0
    - `grep -c 'color-mix' frontend/index.html` returns at least 52 (26 normal + 26 hover branch rules)
    - `.panel-right` selector no longer contains `--bg:` or `--surface:` variable redefinitions
    - `.panel-detail` selector no longer contains `--bg:` or `--surface:` variable redefinitions
    - `grep 'var(--modal-bg)' frontend/index.html` returns at least 1 match
    - `grep 'var(--tooltip-bg)' frontend/index.html` returns at least 1 match
    - `grep 'var(--scrollbar-thumb)' frontend/index.html` returns at least 1 match
    - App loaded in browser with data-theme="mixed" looks identical to before-migration screenshot
  </acceptance_criteria>
  <done>Every hardcoded CSS color value converted to var() reference. 104 data-branch rules consolidated to 52 (26 normal + 26 hover) using color-mix(). Old panel variable override blocks removed. Application appearance unchanged in mixed mode.</done>
</task>

</tasks>

<verification>
1. `grep -cP '#[0-9a-fA-F]{3,8}' frontend/index.html` -- total count should be significantly reduced (only primitives in :root + JS hex values remain)
2. `sed -n '/<style>/,/<\/style>/p' frontend/index.html | grep -vE '^\s*--[a-z]|:root|/\*' | grep -cP '#[0-9a-fA-F]{3,8}'` -- should be 0 (no hex colors in CSS rules, only in variable definitions)
3. `grep -c 'color-mix' frontend/index.html` -- at least 52 (consolidated branch rules)
4. Visual comparison: before-theme-migration.png vs current app appearance (data-theme="mixed")
5. Switch data-theme to "dark" in DevTools -- all panels should go dark
6. Switch data-theme to "light" in DevTools -- all panels should go light
</verification>

<success_criteria>
- Zero hardcoded color values in CSS rules outside :root primitives and theme selector token definitions
- Branch rules consolidated from 104 to 52 (26 normal + 26 hover) using color-mix()
- Old .panel-right/.panel-detail variable overrides removed
- Mixed mode visually identical to pre-migration state
- Dark and light themes render with correct colors when data-theme is changed in DevTools
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-variable-foundation/01-02-SUMMARY.md`
</output>
