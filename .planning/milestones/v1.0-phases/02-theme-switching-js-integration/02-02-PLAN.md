---
phase: 02-theme-switching-js-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/index.html
autonomous: false
requirements:
  - JSIT-01
  - JSIT-03

must_haves:
  truths:
    - "All JS-generated UI elements display correct theme-aware colors in all three themes"
    - "Branch colors in concept lists, tooltips, and detail panels come from CSS variables, not a static JS object"
    - "Confidence tier badges show correct green/amber/red colors in all themes"
    - "No hardcoded hex color values remain in the JS section (except inside getThemeColor fallback chain)"
  artifacts:
    - path: "frontend/index.html"
      provides: "All JS color references migrated to CSS variables or getThemeColor() helper"
      contains: "getBranchColor"
  key_links:
    - from: "JS template literals"
      to: "CSS variable system"
      via: "var(--token) in inline style strings"
      pattern: "var\\(--"
    - from: "BRANCH_COLORS lookups"
      to: "getBranchColor()"
      via: "branchSlug() + getThemeColor()"
      pattern: "getBranchColor\\("
    - from: "canvas ctx.fillStyle/strokeStyle"
      to: "getThemeColor()"
      via: "runtime CSS variable read"
      pattern: "getThemeColor\\("
---

<objective>
Migrate all ~101 hardcoded hex color values in the JavaScript section to use CSS variables (`var(--token)` in template literals) or `getThemeColor()` (for canvas operations), and eliminate the `BRANCH_COLORS` object entirely by replacing all 13 lookups with `getBranchColor()`.

Purpose: Make all JS-generated UI elements respond to theme changes, completing the theme system. Without this, dynamically generated HTML (tooltips, detail panels, concept lists, graph) would show stale colors from the dark/mixed theme regardless of which theme is active.
Output: Zero hardcoded hex colors in JS (outside fallback chains), BRANCH_COLORS object deleted, all dynamic content theme-aware.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-theme-switching-js-integration/02-CONTEXT.md
@.planning/phases/02-theme-switching-js-integration/02-RESEARCH.md
@.planning/phases/02-theme-switching-js-integration/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (must be complete before this plan runs) -->

Theme helper functions available in the JS scope:
```javascript
function getThemeColor(name)      // reads CSS var '--' + name via getComputedStyle
function branchSlug(displayName)  // 'Actor / Player' -> 'actor-player'
function getBranchColor(displayName)  // returns theme-aware branch color via CSS var
```

CSS semantic tokens available for var() in inline styles:
  --text-dim       (replaces #6b6b80, #888, #a0a0b0, #8888a0, #c0c0d0, #666, #aaa, #555)
  --text           (replaces #1a1a2e, #444)
  --accent         (replaces #3b64e0)
  --border         (replaces #d0d0da, #e0e0e8, #d0d2da)
  --surface        (replaces #f6f6fa, #f8f8fa, #f5f6f8)
  --surface2       (replaces #f0f0f4, #eeeef6)
  --red            (replaces #f87171, #dc2626)
  --orange         (replaces #ff9800, #e67e22, #9e6b0a)
  --purple         (replaces #9C27B0, #7c3aed, #6b3fa0)
  --cyan           (replaces #1a7a7a)
  --white          (replaces #fff)
  --conf-high      (replaces #166534)
  --conf-mid       (replaces #92400e)
  --conf-low       (replaces #991b1b)
  --conf-high-bg   (replaces #dcfce7)
  --conf-mid-bg    (replaces #fef3c7)
  --conf-low-bg    (replaces #fee2e2)
  --individual-citation  (replaces #009688)
  --individual-entity    (replaces #e67e22)
  --property-underline   (replaces #9C27B0)
  --bg             (for canvas background)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all JS hex colors to CSS variables and eliminate BRANCH_COLORS</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (lines 3531-3559 for BRANCH_COLORS object, then scan lines 3422-10093 for all hex color references)
    - .planning/phases/02-theme-switching-js-integration/02-RESEARCH.md (lines 287-430 for complete hex-to-token mapping table and migration strategies A/B/C, and pitfalls 3/6/7)
  </read_first>
  <action>
This task is a mechanical search-and-replace across the JS section of `frontend/index.html` (lines ~3422 to ~10093). There are three migration strategies and one deletion. Apply them ALL.

**STRATEGY A: Template literal inline styles** — Replace hex with `var(--token)` directly in template strings. This is used for ALL hex values that appear inside `style="..."` attributes in template literals.

Complete mapping (apply every instance):

| Find (hex) | Replace with | Notes |
|------------|-------------|-------|
| `#6b6b80` | `var(--text-dim)` | ~25 occurrences. Fallback gray text, loading states, secondary labels |
| `#888` | `var(--text-dim)` | ~9 occurrences. Tooltip secondary text |
| `#a0a0b0` | `var(--text-dim)` | ~3 occurrences |
| `#8888a0` | `var(--text-dim)` | ~3 occurrences |
| `#c0c0d0` | `var(--text-dim)` | ~1 occurrence |
| `#666` | `var(--text-dim)` | ~2 occurrences |
| `#aaa` | `var(--text-dim)` | ~1 occurrence |
| `#555` | `var(--text-dim)` | ~1 occurrence |
| `#1a1a2e` | `var(--text)` | ~4 occurrences. Dark text in detail panels |
| `#444` | `var(--text)` | ~1 occurrence |
| `#3b64e0` | `var(--accent)` | ~11 occurrences. Links, highlights, edge strokes. BUT NOT in canvas ctx operations — those use Strategy B |
| `#009688` | `var(--individual-citation)` | ~9 occurrences. Teal citation highlights |
| `#e67e22` | `var(--individual-entity)` | ~4 occurrences. Orange entity indicators |
| `#991b1b` | `var(--conf-low)` | ~6 occurrences. Error/low confidence text |
| `#dcfce7` | `var(--conf-high-bg)` | ~4 occurrences. High confidence background |
| `#fef3c7` | `var(--conf-mid-bg)` | ~4 occurrences. Medium confidence background |
| `#fee2e2` | `var(--conf-low-bg)` | ~4 occurrences. Low confidence background |
| `#166534` | `var(--conf-high)` | ~4 occurrences. High confidence text |
| `#92400e` | `var(--conf-mid)` | ~4 occurrences. Medium confidence text |
| `#d0d0da` | `var(--border)` | ~4 occurrences. Triple borders |
| `#e0e0e8` | `var(--border)` | ~1 occurrence. Tooltip link border |
| `#d0d2da` | `var(--border)` | ~1 occurrence. Graph node stroke |
| `#f6f6fa` | `var(--surface)` | ~2 occurrences. Tooltip backgrounds |
| `#f8f8fa` | `var(--surface)` | ~1 occurrence. Graph node fill |
| `#f5f6f8` | `var(--surface)` | ~1 occurrence. Graph node fill |
| `#f0f0f4` | `var(--surface2)` | ~2 occurrences. Lineage badges |
| `#eeeef6` | `var(--surface2)` | ~1 occurrence. Tooltip hover |
| `#9C27B0` | `var(--property-underline)` | ~3 occurrences. Property purple |
| `#f87171` | `var(--red)` | ~2 occurrences. Danger button |
| `#dc2626` | `var(--red)` | ~1 occurrence. Deprecated badge |
| `#7c3aed` | `var(--purple)` | ~1 occurrence. seeAlso edge |
| `#ff9800` | `var(--orange)` | ~1 occurrence. Passive voice badge |
| `#9e6b0a` | `var(--orange)` | ~1 occurrence. Triple predicate |
| `#1a7a7a` | `var(--cyan)` | ~1 occurrence. Triple subject |
| `#6b3fa0` | `var(--purple)` | ~1 occurrence. Triple object |
| `#fff` | `var(--white)` | ~2 occurrences. White text on badges |

For confidence tier ternaries (Pitfall 6), the pattern is:
```javascript
// Before:
confPct >= 80 ? '#dcfce7' : confPct >= 50 ? '#fef3c7' : '#fee2e2'
// After:
confPct >= 80 ? 'var(--conf-high-bg)' : confPct >= 50 ? 'var(--conf-mid-bg)' : 'var(--conf-low-bg)'
```

For onmouseover/onmouseout inline handlers (Pitfall 7), convert hex to var():
```javascript
// Before:
onmouseover="this.style.background='#eeeef6'"
// After:
onmouseover="this.style.background='var(--surface2)'"
```

**STRATEGY B: Canvas/computed values** — Use `getThemeColor()` for canvas 2D context and SVG attribute operations where `var()` syntax does not work.

In `renderMinimap()` (around line 8785):
```javascript
// Before:
ctx.fillStyle = 'rgba(59,100,224,0.4)';
// After:
const accentHex = getThemeColor('accent');
ctx.fillStyle = accentHex ? accentHex + '66' : 'rgba(59,100,224,0.4)';
// '66' is hex for ~40% opacity
```

In `renderMinimap()` viewport rect (around line 8798):
```javascript
// Before:
ctx.strokeStyle = 'rgba(228,230,240,0.5)';
// After:
const borderHex = getThemeColor('text-dim');
ctx.strokeStyle = borderHex ? borderHex + '80' : 'rgba(228,230,240,0.5)';
// '80' is hex for ~50% opacity
```

For any SVG `setAttribute('stroke', '#hex')` calls in graph rendering, replace with `getThemeColor('accent')` or the appropriate token.

For `rgba(232,165,76,0.15)` (individual layer toggle bg) and `rgba(176,126,232,0.15)` (property layer toggle bg) and `rgba(156,39,176,0.12)` (property tooltip badge bg): replace with `var()` references if in template literals, or `getThemeColor()` if in computed contexts. Use `color-mix(in srgb, var(--individual-entity) 15%, transparent)` or `color-mix(in srgb, var(--property-underline) 15%, transparent)` for the tinted backgrounds.

**STRATEGY C: Branch color lookups** — Replace ALL `BRANCH_COLORS[...]` lookups with `getBranchColor()`.

There are 13 BRANCH_COLORS lookups to replace. For each one:

```javascript
// Before (various patterns):
BRANCH_COLORS[ab] || '#6b6b80'
BRANCH_COLORS[branch] || '#3b64e0'
detail.branch_color || BRANCH_COLORS[detail.branch] || '#6b6b80'
// After:
getBranchColor(ab)
getBranchColor(branch)
getBranchColor(detail.branch)
```

IMPORTANT: The `detail.branch_color` prefix in the fallback chain must be DROPPED. The backend-provided `branch_color` is a static hex (not theme-aware). After migration, ALL branch color lookups go through `getBranchColor()` which reads live CSS variables. This is per decision D-03 from CONTEXT.md.

The 13 locations (from grep):
1. Line ~5588: `BRANCH_COLORS[ab] || '#6b6b80'` -> `getBranchColor(ab)`
2. Line ~5719: `BRANCH_COLORS[clBranch] || '#3b64e0'` -> `getBranchColor(clBranch)`
3. Line ~5846: `BRANCH_COLORS[branch] || '#6b6b80'` -> `getBranchColor(branch)`
4. Line ~6045: `BRANCH_COLORS[ab] || '#6b6b80'` -> `getBranchColor(ab)`
5. Line ~6203: `BRANCH_COLORS[branch] || '#3b64e0'` -> `getBranchColor(branch)`
6. Line ~6282: `BRANCH_COLORS[branch] || '#3b64e0'` -> `getBranchColor(branch)`
7. Line ~7302: `detail.branch_color || BRANCH_COLORS[detail.branch] || '#6b6b80'` -> `getBranchColor(detail.branch)`
8. Line ~7383: `detail.branch_color || BRANCH_COLORS[detail.branch] || '#6b6b80'` -> `getBranchColor(detail.branch)`
9. Line ~7417: `detail.branch_color || BRANCH_COLORS[detail.branch] || '#6b6b80'` -> `getBranchColor(detail.branch)`
10. Line ~7501: `detail.branch_color || BRANCH_COLORS[detail.branch] || '#6b6b80'` -> `getBranchColor(detail.branch)`
11. Line ~7750: `detail.branch_color || BRANCH_COLORS[detail.branch] || '#6b6b80'` -> `getBranchColor(detail.branch)`
12. Line ~8277: `detail.branch_color || BRANCH_COLORS[detail.branch] || '#3b64e0'` -> `getBranchColor(detail.branch)`
13. Line ~9318: `BRANCH_COLORS[branch] || '#6b6b80'` -> `getBranchColor(branch)`

**DELETION: Remove BRANCH_COLORS object.**

Delete the entire `BRANCH_COLORS` object definition (lines ~3531-3559, the `const BRANCH_COLORS = { ... };` block). This is 28 lines of dead code after all lookups are migrated to `getBranchColor()`.

**VERIFICATION AFTER ALL CHANGES:**

Run a search for remaining hex patterns in the JS section (after line ~3422). The ONLY hex values that should remain are:
- `'#8b8fa3'` inside the `getBranchColor()` fallback (this is the default branch color for when CSS var is missing)
- Hex alpha suffixes like `+ '66'` or `+ '80'` used with getThemeColor() return values in canvas operations
- Any hex values that are truly theme-independent (there should be none)

EVERY other hex value in the JS section must be converted to a `var(--token)` or `getThemeColor()` call.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-enrich" && echo "=== BRANCH_COLORS references (should be 0) ===" && grep -c "BRANCH_COLORS" frontend/index.html || true && echo "=== getBranchColor references (should be 13+) ===" && grep -c "getBranchColor" frontend/index.html && echo "=== Remaining raw hex in JS (audit) ===" && awk '/^<script>$/,/^<\/script>$/' frontend/index.html | grep -oE "'#[0-9a-fA-F]{3,8}'" | sort | uniq -c | sort -rn | head -20 && echo "=== var(-- usage in JS (should be many) ===" && awk 'NR>3400' frontend/index.html | grep -c "var(--" && echo "=== Backend tests ===" && cd backend && .venv/bin/python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BRANCH_COLORS" frontend/index.html` returns 0 (object and all references eliminated)
    - `grep -c "getBranchColor" frontend/index.html` returns at least 14 (1 definition + 13 call sites)
    - No hex values like `'#6b6b80'`, `'#3b64e0'`, `'#888'`, `'#009688'` remain in JS section outside of getBranchColor fallback
    - `grep -c "var(--" frontend/index.html` in JS section (lines 3400+) returns at least 80 (replacing ~101 hex values)
    - `grep -c "getThemeColor" frontend/index.html` returns at least 5 (definition + canvas uses)
    - No `detail.branch_color` appears in any color assignment expression
    - Backend tests pass with no regressions
  </acceptance_criteria>
  <done>
    - BRANCH_COLORS object completely removed from codebase
    - All 13 BRANCH_COLORS lookups replaced with getBranchColor() calls
    - All ~101 hardcoded hex values in JS replaced with var(--token) or getThemeColor() calls
    - Canvas minimap uses getThemeColor() for fill/stroke colors
    - Confidence tier ternaries use var(--conf-*) tokens
    - Tooltip hover handlers use var(--surface2) instead of hex
    - detail.branch_color is no longer used in color fallback chains
    - Only remaining hex in JS is the '#8b8fa3' fallback inside getBranchColor()
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all three themes with dynamic content</name>
  <files>frontend/index.html</files>
  <action>
    Human verifies all JS-generated content themes correctly across all three modes:
    1. Enrich a legal document (or use a demo) at http://localhost:8732
    2. Dark theme: verify concept list colors, branch badges, confidence tiers, tooltip text, detail panel colors
    3. Light theme: verify all elements show light-appropriate colors
    4. Mixed theme: verify left panel (dark) and right panel (light) show correct colors
    5. Graph modal: verify nodes/edges render with theme-appropriate colors
    6. Switch themes while graph is open — verify immediate re-render with correct colors
    7. Confidence badges: verify green/amber/red in each theme
    8. Tooltip link hover: verify background color changes per theme
  </action>
  <verify>Visual inspection by user confirms all dynamic content themes correctly in all three modes</verify>
  <done>User has approved that all JS-generated content displays correct theme-aware colors in Dark, Light, and Mixed modes</done>
</task>

</tasks>

<verification>
- Zero BRANCH_COLORS references remain in the file
- All JS hex colors replaced with var(--token) or getThemeColor()
- Canvas operations use getThemeColor() for dynamic color reads
- All three themes render JS-generated content correctly
- Graph re-renders on theme change with correct colors
- Backend tests pass
</verification>

<success_criteria>
No hardcoded hex colors remain in the JS section (except getBranchColor fallback), BRANCH_COLORS object is deleted, and all dynamically generated UI elements display correct colors in all three themes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-switching-js-integration/02-02-SUMMARY.md`
</output>
